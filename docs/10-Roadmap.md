# 10 · Roadmap — সামনে কী, কোন ক্রমে

**লেখা:** ১৪ আগস্ট ২০২৬ · **ভিত্তি:** Hubstaff · Time Doctor · ActivTrak ·
Insightful · Monitask · Kickidler · WorkTime · Kimai/solidtime-এর ফিচার
রিসার্চ + আমাদের নিজের খোলা তালিকা (G/O)

> ⭐ রিসার্চের মূল ফল আগে: **প্রতিযোগীদের মূল ফিচারের প্রায় সবই আমাদের
> ইতিমধ্যে আছে** — র‍্যান্ডম স্ক্রিনশট, idle শনাক্তকরণ, অ্যাপ/সাইট
> ক্যাটাগরি, লাইভ বোর্ড (cockpit), হিটম্যাপ, রিপোর্ট Excel/PDF, পে-রোল
> (proration-সহ), ইমেইল+টেলিগ্রাম অ্যালার্ট, audit log, 2FA, এনক্রিপ্টেড
> ব্যাকআপ, স্টাফের নিজের ডেটা দেখা, auto-update। এই ফাইলটা **ফাঁকগুলোর**
> তালিকা — আর যেগুলো ইচ্ছা করেই করব না, সেগুলোরও।
>
> ⚠️ **নিয়ম আগের মতোই:** পর্দায় দেখা যায় এমন কিছু বানানোর আগে মকআপ
> দেখিয়ে মালিকের অনুমোদন। এই ফাইলে কোনো কোড-সিদ্ধান্ত চূড়ান্ত নয়।

**প্রেক্ষাপট, যেটা ক্রমটা ঠিক করেছে:** মালিকের কথায় আগস্ট **টেস্ট রান** —
*"ami ei mas mane august test run korbo, bug fix krbo. septembor theke zate
hassel free vabe use kora jay."* তাই ধাপ ১-এ কেবল সেই জিনিসগুলো, যেগুলো
সেপ্টেম্বরের প্রথম আসল বেতন-চক্রকে নিরাপদ করে।

---

## ধাপ ১ — সেপ্টেম্বরের আগে *(বেতন-চক্র নিরাপদ করা)*

| # | ফিচার | ধারণা কোথা থেকে | আকার | অবস্থা |
|---|---|---|---|:---:|
| R1 | **মাস বন্ধ করা (payroll lock)** | Time Doctor-এর payroll close | মাঝারি | ⏳ |
| R2 | **ছুটির খাতা (leave calendar)** | সব পণ্যে আছে; আমাদের O3 | মাঝারি+ | ⏳ |
| R3 | **সাপ্তাহিক রিপোর্ট টেলিগ্রামে** | Hubstaff scheduled reports | ছোট+ | ✅ |
| R4 | **বাইরের uptime নজরদারি** | SaaS status pages | ⭐ কোড নেই | ⏳ |
| R5 | **অফসাইট ব্যাকআপ স্বয়ংক্রিয় (G39)** | — | ছোট (ops) | ⏳ |
| R6 | **VPS শক্ত করা** — fail2ban (পোর্ট ২২২২-সহ) + লগইন রুটে rate limit | — | ছোট (ops) | ◐ অর্ধেক — অ্যাপের দিক ✅ ([G116](08-Gap-Analysis.md)), Caddy-র দিক ⏳ |
| R7 | **সরকারি ছুটি ২০২৬–২৭ প্রি-লোড (O2)** | আমাদেরই খোলা তালিকা | ছোট | ✅ |

> ⭐ **১৪ আগস্ট ২০২৬-এর অবস্থা:** R3 ও R7 কোডে সম্পন্ন, R6-এর fail2ban অংশটুকু
> সম্পন্ন। বাকি চারটে অপরিবর্তিত।
>
> ⚠️⚠️ **"সম্পন্ন" মানে লেখা ও টেস্টে যাচাই — মাঠে চালিয়ে দেখা নয়।** সাপ্তাহিক
> বার্তাটা আজ পর্যন্ত একবারও পাঠানো হয়নি, আর শুক্রবার সন্ধ্যা ছাড়া হাতে
> চালানোর কোনো পথও নেই ([G113](08-Gap-Analysis.md))। ⭐ আজ (১৪ আগস্ট)
> **শুক্রবারই** — অর্থাৎ প্রথম আসল বার্তাটা আজ ১৮:০০-এ, আর তার উইন্ডো
> ৮–১৪ আগস্ট, যার ৮–১২ সার্ভার দেখেইনি (ট্র্যাকিং বসেছে ১৩ আগস্ট)।
> এজেন্টের রোলআউটের মতোই — লেখা শেষ হওয়া আর কাজ করা এক নয়।

### R1 · মাস বন্ধ করা — কেন এটা সবার আগে, আর R7-এর পর **আরও** আগে

মাস শেষে owner এক ক্লিকে মাসটা **বন্ধ** করবেন → `monthly_summary`-র
সংখ্যাগুলো একটা স্ন্যাপশট টেবিলে জমে যাবে, আর বন্ধ মাসে সময়-সংশোধন
(`time_adjustments`) ঢুকতে চাইলে ৪০৯।

- ⚠️ এটা ছাড়া সেপ্টেম্বরে কেউ আগস্টের ঘণ্টা এডিট করলে **দিয়ে দেওয়া
  বেতনের হিসাবই** নীরবে বদলে যেত — আর কোন সংখ্যায় বেতন হয়েছিল সেটা
  প্রমাণ করার উপায় থাকত না।
- ⚠️⚠️ **R7 বসার পর আরেকটা দরজা খুলেছে — ছুটির তারিখ।** ঘণ্টা কেউ না
  ছুঁলেও এখন **ছুটি নড়লেই** পুরোনো মাসের হিসাব পিছন ফিরে বদলায়। পথটা
  মাপা: `refreshMonth()` (`src/summary/summary.service.ts`) প্রতিবার ওই
  মাসের `holidays` টেবিল **নতুন করে পড়ে** আর `prorate()` দিয়ে d ও D আবার
  গোনে, তাই `monthly_summary`-র `target_sec` · `expected_sec` ·
  `expected_workdays` (d) · `month_workdays` (D) — চারটেই **ওই মুহূর্তের**
  ছুটির তালিকার উপর দাঁড়ানো। পে-রোল ওই সারি থেকেই d ও D পড়ে
  (`payroll.service.ts` নিজে গোনে না), অর্থাৎ একটা তারিখ নড়লে
  **সরাসরি টাকা নড়ে**।
  - ⚠️ অতীত মাস আপাতত স্থির থাকে, কিন্তু **সুরক্ষিত নয়**: ওই মাসের কোনো
    সময়-সংশোধন অনুমোদন বা বাতিল হলেই (`adjustments.service.ts` →
    `refreshDate()` → `refreshMonth()`) সারিটা নতুন ছুটির তালিকায় **হঠাৎ
    লাফ দেয়** — যে বদলটা কেউ চায়নি, কেউ দেখেও না।
  - ⭐ seed নিজে সাবধান: সম্মতি (`SEED_HOLIDAYS_PAST=true`) ছাড়া চলতি বা
    অতীত মাসে একটা তারিখও বসায় না, আর কোনগুলো বসল না তা নাম ধরে ছাপে।
    ⚠️ কিন্তু ওটা **seed-এর** প্রহরী — Settings → Holidays পাতায় মালিকের
    হাতের একটা এডিটের সামনে কোনো প্রহরী নেই। ২০২৭-এর প্রজ্ঞাপন বেরোলে
    (নভেম্বর ২০২৬) ঠিক ওই এডিটগুলোই করতে হবে।
  - ⚠️ ২৬ আগস্ট ২০২৬-এর ঈদে মিলাদুন্নবী এখনো **আনুমানিক**। ঘোষণা এসে
    তারিখটা সরলে আগস্টেরই D বদলায় — অর্থাৎ **টেস্ট মাসের** সংখ্যা, যেটা
    দিয়ে সেপ্টেম্বরের প্রথম আসল বেতন-চক্রের নিয়ম যাচাই করার কথা।
  - ⭐ **উপসংহার:** R1 না থাকা পর্যন্ত "গত মাসের হিসাব" বলে স্থির কিছু
    নেই — কেবল আজকের ছুটির তালিকা দিয়ে আজ গোনা একটা সংখ্যা আছে।
- ⭐ পে-রোল রিপোর্ট তখন স্ন্যাপশট থেকে পড়বে, লাইভ টেবিল থেকে নয় —
  "রিপোর্ট আবার নামালে সংখ্যা বদলে গেছে" জাতের অভিযোগ জন্মের আগেই শেষ।
- খোলা প্রশ্ন (মালিকের): বন্ধ করার অধিকার কি শুধু owner? *(প্রস্তাব: হ্যাঁ)*

### R2 · ছুটির খাতা — O3-এর নিষ্পত্তি, কিন্তু নকশার সীমানাসহ

Settings-এ owner **নিজে** কারো ছুটি লেখেন: কর্মী, তারিখ (একদিন বা সীমা),
ধরন (casual/sick), নোট। ওই দিন তার টার্গেট থেকে বাদ (বিদ্যমান
`isOffDay`/ছুটির যন্ত্রপাতিই খাটে — G37-এর proration অটুট), কার্ডে ও
রিপোর্টে "On leave"।

- ⚠️⚠️ **কোনো আবেদন-অনুমোদন প্রবাহ নয়।** "স্টাফের চাপার মতো কোনো বোতাম
  নেই" এই সিস্টেমের ঘোষিত নিয়ম ([README](../README.md) § নিয়ম 3b) —
  স্টাফ মুখে/ফোনে বলবেন, owner লিখবেন। এটুকুই।
- ⭐ প্রতিটা এন্ট্রি audit log-এ (কে লিখল, কবে, কার ছুটি) — বেতনে প্রভাব
  ফেলা প্রতিটা জিনিসের মতোই।
- ⚠️ ছুটির দিনে কেউ কাজ করলে ঘণ্টা **গোনা হবেই** (§ ২.১-খ-এর নিয়ম
  ছুটিতেও এক) — ছুটি মানে টার্গেট মাফ, কাজ নিষেধ নয়।
- খোলা প্রশ্ন (O3-এরই বাকি অংশ): বছরে কোটা থাকবে কি (১০ casual + ১৪
  sick ডিফল্ট ধরা আছে), নাকি শুধু খাতা?

### R3 · সাপ্তাহিক রিপোর্ট টেলিগ্রামে — ✅ সম্পন্ন

প্রতি শুক্রবার ১৮:০০ (ঢাকা) বটে একটা সারাংশ: শেষ ৭ দিনে কার কত ঘণ্টা, কে
টার্গেট-পথে, কে পিছিয়ে, দলের মোট। কোড:
`server/src/digest/weekly.{rules,service,job}.ts` — গোটা হিসাবটা
`weekly.rules.ts`-এ খাঁটি ফাংশনে, তাই DB ছাড়াই টেস্ট হয়
(`test/weekly-digest.spec.ts`)। বিস্তারিত আচরণ [04-Features F07b](04-Features.md)।

- ⭐ নতুন অবকাঠামো লাগেনি, যেমন ভাবা হয়েছিল: `DigestModule` + `TelegramChannel`
  + `ReportsService` (F01+F02) — সংখ্যা **সব রিপোর্ট থেকেই** আসে, তাই
  টেলিগ্রাম আর Reports পাতা কখনো দুই ঘণ্টা বলে না।
- ⚠️ **যেটা করা হয়নি:** উপরে লেখা ছিল "আর ইমেইলে Excel সংযুক্তি" — সেটা
  **হয়নি**। বার্তাটা কেবল টেলিগ্রামে, কোনো সংযুক্তি নেই। দৈনিক ডাইজেস্ট
  (F07) আগের মতোই ইমেইল-only; দুটো জব আলাদা, কেবল মডিউলটা এক।
- ⭐⭐ "পাঠানো হবে শুধু owner-এর chat id-তে" — কথাটা তিন জায়গায় **লেখা**
  ছিল, কোথাও **প্রহরী** ছিল না। এখন আছে: `weeklyGateOf()` পাঠানোর ঠিক
  আগে দাঁড়ায়, আর `TELEGRAM_CHAT_ID` ব্যক্তিগত চ্যাটের চেহারার না হলে
  (ধনাত্মক পূর্ণসংখ্যা নয়) বার্তা **যায় না** — কারণটা লগে পুরো লেখাসহ।
  ⚠️ চ্যাট আইডিটা অ্যালার্টের সাথে ভাগ করা, আর অ্যালার্টে কেবল হোস্টনেম
  যায় বলে ওখানে দলের গ্রুপ বসানো খুবই স্বাভাবিক — তখন প্রথম শুক্রবারেই
  নাম-ধরে-র‍্যাঙ্কিং ওই গ্রুপে চলে যেত।
  ⭐ সজ্ঞানে চাইলে `WEEKLY_DIGEST_ALLOW_GROUP=true` — পথ খোলা, দুর্ঘটনা বন্ধ।
- ⭐⭐ **"রেকর্ড নেই" ≠ "০ ঘণ্টা কাজ"** — এটাই R3-এর সবচেয়ে দামি নিয়ম।
  যে দিন সার্ভার দেখেইনি, তার টার্গেট প্রত্যাশাতেও নেই, ঘাটতিতেও নেই;
  আর যাঁর একটাও দিন দেখা হয়নি তিনি "Behind" নয়, আলাদা **"Not observed"**
  ঘরে যান। ⚠️ নইলে আজকের প্রথম বার্তাটাই নাম ধরে ধরে বলত "৩২ ঘণ্টা
  পিছিয়ে" — এমন দিনের জন্য যখন মাপার যন্ত্রটাই বসেনি।
- ⚠️ **এখনো বাকি ([G113](08-Gap-Analysis.md)):** হাতে চালানোর পথ নেই।
  বার্তা যায় কি না জানার একমাত্র উপায় শুক্রবার সন্ধ্যা, আর ব্যর্থ হলে
  পরের সুযোগ **সাত দিন** পরে।

### R4 · বাইরের uptime নজরদারি — কোড ছাড়াই

UptimeRobot (ফ্রি টিয়ার, ৫ মিনিট পরপর) `https://hub.oxeio.com/api/v1/health`
দেখবে; ডাউন/আপ হলে টেলিগ্রামে।

- ⭐ **সার্ভার নিজে মরে গেলে নিজের অ্যালার্ট পাঠাতে পারে না** — ভেতরের সব
  অ্যালার্ট ব্যবস্থার এই অন্ধ জায়গাটা কেবল বাইরের চোখেই ঢাকে।
- ⚠️ owner-এর একটা ফ্রি অ্যাকাউন্ট লাগবে — সেটআপ ১০ মিনিটের, রানবুকে
  ধাপগুলো লিখে দেওয়া হবে।

### R5 · অফসাইট ব্যাকআপ (G39-এর নিষ্পত্তি)

রাতের এনক্রিপ্টেড ব্যাকআপ ইতিমধ্যেই হয় ও restore-প্রমাণিত; ফাঁকটা হলো
সবকিছু **এক মেশিনে**। `rclone` দিয়ে সাপ্তাহিক Google Drive-এ আপলোড,
সফল/ব্যর্থ টেলিগ্রামে, পুরোনো কপি স্বয়ংক্রিয় ছাঁটাই (শেষ ৮ সপ্তাহ)।

- ⚠️ ফাইলগুলো **আগে থেকেই এনক্রিপ্টেড** (`BACKUP_PASSPHRASE`), তাই Drive-এ
  তুললেও কর্মীদের ছবি/ডেটা Google পড়তে পারে না — নীতির সাথে সাংঘর্ষিক নয়।
- ⚠️ rclone-এর Google অনুমোদন owner-কেই দিতে হবে (এক-বারের কাজ)।
- ⭐ সাথে **ত্রৈমাসিক restore-মহড়া** রানবুকে: ব্যাকআপ থাকা আর ফেরানো
  যাওয়া এক নয় — সেটা আমরা নিজেরাই একবার মাঠে শিখেছি।

### R6 · VPS শক্ত করা — ◐ অর্ধেক

দুটো কাজ ছিল, **একটা হয়েছে**।

**✅ হয়েছে — `deploy/vps-harden.sh`:** fail2ban (SSH — পোর্ট **২২ ও ২২২২**
দুটোই, ⚠️ ডিফল্ট জেল শুধু ২২ দেখে) + **শুধু-নিরাপত্তার** স্বয়ংক্রিয়
আপডেট, আর শেষে সত্যিই চালু আছে কি না মিলিয়ে দেখা।

- ⭐ বারবার চালানো নিরাপদ, আর কিছু না বদলালে সার্ভিস restart-ও করে না —
  restart-এর মুহূর্তে জেল ফাঁকা থাকে।
- ⚠️⚠️ স্ক্রিপ্টটা **sshd-তে হাত দেয় না** — পোর্ট বদলায় না, পাসওয়ার্ড-লগইন
  বন্ধ করে না, ফায়ারওয়ালের নিয়ম মোছে না। ইচ্ছাকৃত: "শক্ত করা"-র নামে
  নিজের দরজা বন্ধ করে ফেলাটা এখানে একবার প্রায় ঘটেছে
  (`deploy/README` § ১২.৪ — ২২ পোর্টের পথ বন্ধ, ফেরার রাস্তা কেবল
  ওয়েব কনসোল)।
- ⚠️ **চালানো হয়নি এখনো** — স্ক্রিপ্টটা রিপোতে আছে, VPS-এ চালানো বাকি।

**⏳ বাকি — Caddy-তে `/api/v1/auth/*`-এর rate limit
([G116](08-Gap-Analysis.md)):** আর এটা "ঐচ্ছিক দ্বিতীয় অংশ" নয়।

- ⚠️⚠️ fail2ban-এর ব্যান বসে `INPUT` চেইনে, কিন্তু Docker-এর প্রকাশিত
  ৮০/৪৪৩ DNAT হয়ে `FORWARD`/`DOCKER` চেইন দিয়ে যায় — অর্থাৎ **ওয়েব-স্তরের
  ব্রুট-ফোর্স এই জেলের বাইরে**, আর ব্যর্থতাটা নীরব:
  `fail2ban-client status sshd` দিব্যি "সক্রিয়" দেখাবে।
- ⭐ দরজাটা পুরো খোলা নয় — অ্যাপে `login-throttle` আছে (I11/G21);
  ⚠️ কিন্তু তার চাবি (ইমেইল + IP) **জোড়া**, তাই এক IP থেকে হাজারটা আলাদা
  ইমেইল চেষ্টা করলে কোনো তালাই পড়ে না, আর কাউন্টারটা ইন-মেমরি — সার্ভার
  রিস্টার্টেই মুছে যায়।
- ⚠️ সীমাটা এমন হতে হবে যাতে দলের সকালবেলার লগইনগুলো আটকে না যায়: সবাই
  একই অফিস-IP-র পেছনে, তাই সার্ভারের চোখে ওটা একটা মেশিন।

### R7 · সরকারি ছুটি প্রি-লোড — ✅ সম্পন্ন

সমস্যাটা ছিল সক্রিয় ভুল: চান্দ্র ছুটিগুলো (ঈদ-সহ) DB-তে না থাকায় ওই
দিনগুলো কর্মদিবস ধরে **টার্গেট ও pace দুটোই বেশি** গোনা হচ্ছিল; seed-এ
ছিল কেবল ৭টা নির্দিষ্ট-তারিখের ছুটি। এখন তালিকাটা
`server/prisma/holidays.data.ts`-এ — **২০২৬-এর ২৮টি ও ২০২৭-এর ২৩টি সারি**,
যার **৩৩টি আনুমানিক**। খাঁটি ফাংশন, তাই DB ছাড়াই টেস্ট হয়
(`test/holidays.spec.ts`)।

- ⭐⭐ **আনুমানিক তারিখ আনুমানিক বলেই যায়:** চান্দ্র ও তিথি-নির্ভর ছুটির
  দিন ঘোষণার পর নড়ে, তাই ওই সারির নামের শেষে DB-তে **"(সম্ভাব্য)"** জুড়ে
  বসে — মালিক Settings → Holidays পাতায় **চোখেই দেখতে পান** কোনটা পাকা নয়।
  ⚠️ চিহ্নটা `type` ঘরে বসানো হয়নি: পর্দার Type বাছাইয়ে মাত্র তিনটে মান
  আছে, তাই Edit → Save চাপলেই চিহ্নটা নীরবে হারিয়ে যেত।
- ⭐⭐ **seed কখনো কিছু বদলায় না বা মোছে না** — কেবল অনুপস্থিত সারি বসায়।
  নইলে মালিক ঘোষণা দেখে হাতে ঠিক করা নামটা পরের `db seed`-এ আবার
  "(সম্ভাব্য)" হয়ে ফিরত। মেলানো হয় তারিখে, নামে **ও গুচ্ছে** (ঈদ ও পূজার
  পরপর দিনগুলো এক-একটা গুচ্ছ) — নইলে মালিক ঈদ একদিন সরালে seed খালি
  তারিখটায় আরেকটা ঈদ বানিয়ে দিত, অর্থাৎ **একটা বাড়তি ছুটি**।
- ⚠️⚠️ **চলতি ও অতীত মাসে সম্মতি ছাড়া কিছুই বসে না** (`SEED_HOLIDAYS_PAST`)
  — কারণ একটা ছুটি বসলেই ওই মাসের D কমে, আর তাতে `target_sec` ·
  `expected_sec` · `pace_sec` আর পে-রোলের `d ÷ D` সবই নড়ে (§ R1)। কোনগুলো
  বসল না, seed তা **নাম ও তারিখ ধরে** ছাপে — "বসানো হয়নি" আর "বসানোর
  দরকার নেই" এক কথা নয়।
  - ⚠️ ফলে আজ চালালে আগস্টের অনুপস্থিত সারিগুলো (**২৬ আগস্টের ঈদে
    মিলাদুন্নবী**-সহ) বসবে **না** — চলতি মাসও "হিসাব চলে গেছে" ধরা হয়।
- ⚠️⚠️ **২০২৭-এর প্রজ্ঞাপন এখনো বেরোয়নি** (সাধারণত নভেম্বরে) — ওই বছরের
  চান্দ্র তারিখগুলো জ্যোতির্গণনার হিসাব, সরকারি সিদ্ধান্ত নয়। seed প্রতিবার
  কয়টা তারিখ ও কবে মেলাতে হবে সেটা গুনে বলে (`PENDING_GAZETTES`)।
  ⭐ প্রজ্ঞাপন এলে তালিকা ঠিক করে বছরটা ওই তালিকা থেকে **সরিয়ে দিতে হবে**,
  নইলে সতর্কবার্তাটা মিথ্যে হয়ে বাজতেই থাকত।
- ⚠️ **VPS-এ এখনো `db seed` চালানো হয়নি** — উপরের সংখ্যাগুলো তালিকার,
  DB-র নয়। চালানোর আগে `deploy/README.md § ২.১গ` পড়া।
- ⏳ **যা বাকি রয়ে গেল:** [G108](08-Gap-Analysis.md) — "(সম্ভাব্য)" চিহ্নটা
  রিপোর্ট, Excel, PDF বা পে-রোল পাতার কোথাও পৌঁছায় না, অথচ ওখানেই `d ÷ D`
  দিয়ে টাকা কাটা হয়; [G114](08-Gap-Analysis.md) — `settings['seed.holidays']`
  দেখা বা বদলানোর একমাত্র পথ `psql`; [G115](08-Gap-Analysis.md) —
  `nameEn` বাধ্যতামূলক ও যাচাই করা, কিন্তু কোথাও দেখানো হয় না।
- ⭐ R2-র (ছুটির খাতা) আগে এটা ছিল — ব্যক্তিগত ছুটির হিসাব সঠিক হতে হলে
  আগে **সবার** ছুটির দিনগুলো সঠিক হতে হয়। সেই ভিত এখন আছে।

---

## ধাপ ২ — সেপ্টেম্বরে, ডেটা জমার পর

| # | ফিচার | ধারণা কোথা থেকে | আকার | অবস্থা |
|---|---|---|---|---|
| R8 | **অতিরিক্ত-কাজের সংকেত (burnout card)** | WorkTime/TraqNext | মাঝারি | ⏳ |
| R9 | **মনোযোগের মাপ (focus metrics)** | ActivTrak | মাঝারি | ⏳ |
| R10 | **সপ্তাহের শীর্ষ অ্যাপ/সাইট** | Hubstaff/Insightful | ছোট+ | ⏳ |
| R11 | **মাসে-মাসে তুলনা** | — (পরিকল্পিতই ছিল) | ছোট | ⏳ |
| R12 | **ফোনে ইনস্টলযোগ্য ড্যাশবোর্ড (PWA)** | সবার mobile app | ছোট | ✅ |
| R13 | **দিনের ফিল্মস্ট্রিপ (গ্যালারি)** | Monitask timeline | ছোট+ | ⏳ |
| R14 | **স্টাফের মাসিক স্টেটমেন্ট PDF** | Kimai-র per-user report | ছোট+ | ⏳ |
| R15 | **পে-রোলে ওভারটাইম আলাদা দেখানো** | O4-এর সিদ্ধান্ত মেনে | ছোট | ⏳ |

- **R8** — শুধু **ঘণ্টার ধাঁচ** থেকে, কোনো কনটেন্ট নয়: টানা বিরতিহীন
  দিনের স্ট্রিক, রাতের (২১:০০+) কাজের ভাগ বাড়া, সাপ্তাহিক ছুটিতেও
  নিয়মিত কাজ। বোর্ডে শান্ত একটা কার্ড — শাস্তির সুর নয়, খবরের।
  ⚠️ সংকেতগুলোর সীমা (কত দিনের স্ট্রিক = সংকেত) মালিকের অনুমোদনে।
- **R9** — বিদ্যমান `app_usage` থেকেই: দিনের দীর্ঘতম অবিচ্ছিন্ন active
  টান, ঘণ্টায় অ্যাপ-বদলের হার। ⭐ **এজেন্টে হাত দিতে হয় না।**
- **R12 ✅ (১৪ আগস্ট ২০২৬)** — আলাদা অ্যাপ নয়, ড্যাশবোর্ডটাই ইনস্টলযোগ্য:
  manifest + ছ-টা আইকন + সার্ভিস ওয়ার্কার, **শূন্য নতুন dependency**।
  ⭐⭐ সবচেয়ে জরুরি সিদ্ধান্ত — **SW কোনো API উত্তর ক্যাশ করে না**। এটা
  লাইভ ড্যাশবোর্ড, তাই অফলাইনে গতকালের সংখ্যা দেখানো মানে সংখ্যাকে মিথ্যা
  বলানো; আর `/api/v1/screenshots/*` কর্মীর পর্দার ছবি, ফোনের ক্যাশে ওগুলো
  জমা করা যাবে না। তিন স্তরে আটকানো: `fetch` হ্যান্ডলারে `/api` সবার আগে
  বাদ · রানটাইমে কোথাও `cache.put` নেই (ক্যাশ `install`-এ একবার লেখা হয়ে
  read-only) · আর বিল্ড-টাইম গার্ড, precache তালিকায় `/api` ঢুকলে বিল্ড
  থামে। ⚠️ `vite-plugin-pwa` নেওয়া হয়নি — workbox ডিফল্টে অনেক কিছু
  runtime ক্যাশ করে, আর "ডিফল্ট বন্ধ করেছি" ভরসার চেয়ে নিজের চোখে পড়া
  ৫০ লাইন এখানে নিরাপদ।
  ⭐ সাথে ফোনের তিনটে **আসল ভাঙন** সারানো হয়েছে: iOS-এ ইনপুটে ফোকাস দিলেই
  পাতা জুম হয়ে যেত (সব ইনপুট ১৬px-এর ছোট), Safari ট্যাপে ফোকাস দেয় না বলে
  দুটো চার্টে সংখ্যা উঠত না, আর ভার্সন ব্যাজ অন্যের ট্যাপ গিলে ফেলত।
  ⚠️ **যা প্রমাণিত নয়:** আসল ফোনে ইনস্টল। লাইভ সাইটে SW `activated` ও
  ক্যাশে `/api`-র কিছু নেই — সেটুকু মাপা; কিন্তু হোমস্ক্রিনে বসানোর
  অভিজ্ঞতা কেউ এখনো পার করেনি।
  ⚠️ ফোনে **ব্যবহারযোগ্যতা**-র সাতটা প্রশ্ন এখনো খোলা ([G121](08-Gap-Analysis.md)–[G124](08-Gap-Analysis.md)) —
  বিশেষ করে মাসিক হিটম্যাপের ১৮px ঘর, যা ফোনে প্রায় অকেজো।
- **R15** — টার্গেট-ছাড়ানো ঘণ্টা পে-রোল পাতায় আলাদা কলামে। ⚠️ O4-এর
  উত্তর অনুযায়ী **শুধু দেখানো, টাকায় গুণ নয়** — ওভারটাইমের আলাদা রেট
  নেই বলে সংখ্যাটাই যথেষ্ট।
- **R14** — `/me`-তে স্টাফ নিজের মাসের PDF নামাতে পারবেন (ঘণ্টা, ছুটি,
  টার্গেট) — স্বচ্ছতার দিকটা পূর্ণ হয়; বেতনের ঘর **থাকবে না** (ADR-023:
  বেতন শুধু owner দেখেন, আর নিজের বেতন সবাই এমনিও জানেন — তবু সার্ভারে
  ওই পথ খোলা হবে না)।

## ধাপ ৩ — পরে, দরকার বুঝে

| # | ফিচার | নোট |
|---|---|---|
| R16 | টেলিগ্রামে দুই-মুখী কমান্ড (`/today`, `/week`, `/who`) | ⚠️ শুধু owner-এর chat id সাড়া পাবে — নইলে বটের username জানা যে-কেউ দলের সংখ্যা টেনে নিত |
| R17 | AI সাপ্তাহিক সারাংশ বাংলায় | সংখ্যা→দু-প্যারা; ⚠️ API খরচ আছে, আর সংখ্যার বাইরে কোনো কনটেন্ট মডেলকে পাঠানো হবে না |
| R18 | স্ক্রিনশটে নোট/ফ্ল্যাগ | "এটা নিয়ে কথা বলব" চিহ্ন; audit-এ লেখা |
| R19 | G82 — সাইন-আউট বনাম PC বন্ধ আলাদা দেখা | ⚠️ অর্ধেকটা এজেন্টে (C#) — Windows মেশিনে বসে করার কাজ |
| R20 | মাউস-জিগলার সংকেত (G46-এর গৃহীত পথ) | দীর্ঘ **অবিচ্ছিন্ন** ACTIVE (লক নেই · সেশন-বদল নেই · foreground-বদল নেই) রিভিউয়ের জন্য চিহ্নিত। ⚠️ hook/keylogger দিয়ে **নয়** — ওটা প্রত্যাখ্যাত |

---

## যেগুলো ইচ্ছা করেই করব না

রিসার্চে বারবার এসেছে, তবু বাদ — কারণসহ, যাতে ছ-মাস পরে কেউ "ওদের তো
আছে" বলে আবার না তোলে:

| প্রতিযোগীর ফিচার | কেন নয় |
|---|---|
| ভিডিও/স্ক্রিন রেকর্ডিং, লাইভ স্ক্রিন দেখা (Kickidler) | [README-র "কখনোই নয়"](../README.md) — নজরদারি আর অত্যাচারের সীমারেখা এখানেই |
| Keylogger, keystroke cadence, sentiment analysis | ওই একই তালিকা — কনটেন্ট পড়া নিষিদ্ধ |
| স্টাফের tray-তে pause/break বোতাম | "স্টাফের চাপার মতো কোনো বোতাম নেই" — বোতাম দিলে ২০৮ ঘণ্টার মানেই যায় |
| GPS/geofencing (Hubstaff) | সবাই অফিসে, দরকারই নেই — আর ব্যক্তিগত অবস্থান এই সিস্টেমের বিষয় নয় |
| ছুটির আবেদন-অনুমোদন প্রবাহ | অনুমোদন ব্যবস্থা নিষিদ্ধ (নিয়ম 3b) — R2-তে owner সরাসরি লেখেন |
| Client billing/invoice (Kimai) | এটা ভেতরের অফিস, ক্লায়েন্ট বিলিং নেই |

---

## ঝুলে থাকা সিদ্ধান্ত, এই roadmap-এর সাথে জড়িত

| কী | অবস্থা |
|---|---|
| আগস্টের pace "ট্র্যাকিং শুরুর দিন থেকে" (মালিক-অনুমোদিত) | ✅ **হয়ে গেছে — এই সারিটা আর ঝুলে নেই।** ⚠️ আগে এখানে "⏸️ ইচ্ছা করে ফেলে রাখা" লেখা ছিল, অথচ এই ব্যাচ ঠিক সেটাই বাস্তবায়ন করেছে; লেখাটা কোডের সাথে **সরাসরি বিরোধী** হয়ে পড়েছিল। এখন `summary.math.ts`-এর `elapsedWindow()` জানালা শুরু করে max(পর্বের শুরু, `joined_on`, **তার নিজের** ট্র্যাকিং-শুরু) থেকে আর শেষ করে min(গতকাল, পর্বের শেষ, `left_on`)-এ; `summary.service` · `progress.service` · `reports.service` তিনটেই এই একটাই ফাংশন ডাকে, আর Live Board-এর সাত-দিনের ফিতে একই চারটে সীমা মেনে চলে (⚠️ কেবল আজকের দিনটা রাখে — প্রশ্নটা আলাদা, `trendDayExpectation()`-এর নোট)। ফলে ১–১২ আগস্টের না-দেখা দিনগুলো আর কারো ঘাটতিতে নেই — tray, Live Board, Monthly, রিপোর্ট ও ডাইজেস্ট সবখানে এক। ⏳ ⚠️ তবু তিনটে ধার বাকি: জানালার **মাঝখানের** ফাঁক এখনো ঘাটতি ([G109](08-Gap-Analysis.md)), Monthly হিটম্যাপে ওই দিনগুলো এখনো লালচে ([G110](08-Gap-Analysis.md)), আর "এখনো দেখা হয়নি" আর "টার্গেট পূরণ" তিন পর্দায় দেখতে এক ([G111](08-Gap-Analysis.md)) |
| বেতন ও যোগদানের তারিখ বসানো | মালিক করবেন (১৪ আগস্ট বলেছেন আজকের মধ্যে) — R1/R2-র হিসাব এগুলোর উপর দাঁড়াবে |
| `.pfx` ব্যাকআপ · trust-publisher.ps1 | মালিকের হাতে, রোলআউটের আগে |
| VPS-এ `_purged_*` টেবিল তিনটে (মোছা নমুনা কর্মীর ব্যাকআপ) | কিছুদিন পরে নিশ্চিন্তে ফেলা যাবে |
| Claude-র SSH কী (`claude-code-oxeio-2026-08-13` ট্যাগ) | দরকার ফুরোলে `authorized_keys` থেকে মোছা |
| ⚠️⚠️ **Monitoring policy-তে স্টাফের সই** | Phase 7-এর **বাধ্যতামূলক** শর্ত ([01-Planning](01-Planning.md)) — অথচ ১২ জন আসল স্টাফ **ইতিমধ্যে ট্র্যাক হচ্ছেন**। আইনি ও আস্থার দিক থেকে এটা roadmap-এর যেকোনো ফিচারের আগে; টেমপ্লেট তৈরিই আছে ([monitoring-policy-template](monitoring-policy-template.md)) |
| O1 — রিমোট/WFH স্টাফ | এখনো কেউ নেই; কেউ এলে তবেই ভাবা (Phase 6) |

---

*রিসার্চ-সূত্র: Hubstaff features · Time Doctor/ActivTrak/Insightful
তুলনা · Monitask all-features · Kickidler · WorkTime non-invasive
monitoring · Kimai vs solidtime · burnout-detection লেখাগুলো (worktime,
webwork, traqnext) — লিঙ্কসহ তালিকা ১৪ আগস্টের চ্যাট-নোটে।*
