# 08 · Gap Analysis

পুরো প্ল্যান দুই দফায় শুরু থেকে যাচাই করা হয়েছে, আর কোড লিখতে, চালাতে ও ডিপ্লয় করতে গিয়ে আরও কিছু বেরিয়েছে। **মোট ৮০টি।**

- **G1–G28** — প্রথম দফা (§ A–F): ডিজাইন সম্পূর্ণ করার সময়
- **G29–G40** — [দ্বিতীয় দফা (§ G)](#-g--দ্বিতীয়-দফা-রিভিউ--কোড-শুরুর-ঠিক-আগে-g29g40): কোড শুরুর ঠিক আগে, শিফট ও অনুমোদন বাদ দেওয়ার **পরে** তৈরি হওয়া নতুন ফাঁক
- **G41–G80** — § G.3: কোড লিখতে ও টেস্ট করতে গিয়ে যা বেরিয়ে এল (G44–G46 এসেছে Win32 API
  গবেষণা থেকে, যেখানে ১৭টি দাবির ১৫টিই ভুল বা আংশিক ভুল প্রমাণিত হয়েছিল; G72–G77 এসেছে
  সত্যিই চালানো ও ডিপ্লয় করার চেষ্টা থেকে)

  ⭐ **G78–G80 এসেছে মালিকের হাতে MSI বসানো থেকে** — দুটোই এমন ভুল যা
  ৪১৮টা এজেন্ট-টেস্টের একটাও ধরেনি, কারণ দুটোরই জন্ম **জোড়ার মুখে**:
  ইনস্টলার টাস্ক বানাত কিন্তু চালাত না, আর ট্র্যাকিং শুরু হতো enrollment-এর
  অপেক্ষা না করেই।

---

## 🔴 A · সঙ্কটজনক ফাঁক (এগুলো ছাড়া সিস্টেম ভুল ফল দিত)

### G1 · Clock drift — কেউ PC-র ঘড়ি বদলে দিলে
**সমস্যা:** সব টাইমস্ট্যাম্প এজেন্টের ঘড়ি থেকে নেওয়া হচ্ছিল। কারো PC-র ঘড়ি ভুল থাকলে (বা কেউ ইচ্ছে করে পিছিয়ে দিলে) পুরো টাইম হিসাব নষ্ট — অথচ কেউ টেরই পেত না।
**সমাধান:** প্রতি রিকোয়েস্টে `client_time` বনাম `server_time` মিলিয়ে drift বের করা, সংশোধন করে সেভ, ৫ মিনিটের বেশি হলে অ্যালার্ট। সেগমেন্টের দৈর্ঘ্য মাপা হবে **monotonic clock** দিয়ে, যেটা ঘড়ি বদলালেও বদলায় না।
📄 [02-Workflow §2](02-Workflow.md) · [07-Spec `devices.last_drift_sec`](07-Technical-Spec.md)

### G2 · ছুটি ও সরকারি ছুটির ক্যালেন্ডার — ডেটাবেসেই ছিল না
**সমস্যা:** ছুটির দিনে বা কেউ ছুটি নিলে সিস্টেম তাকে **"অনুপস্থিত"** দেখাত। অ্যাটেনডেন্স রিপোর্ট কার্যত অকেজো হয়ে যেত।
**তখনকার সমাধান:** ৪টি নতুন টেবিল যোগ করা হয়েছিল।
**⚠️ পরে অপ্রাসঙ্গিক হয়ে গেছে:** মাসিক-ঘণ্টা মডেলে "অনুপস্থিত" বলে কিছু নেই — শুধু ঘণ্টা গোনা হয়। তাই leave টেবিলগুলো বাদ (দফা ৪, নিচে § D3)। শুধু `holidays` থাকছে, হিটম্যাপে দিন চিহ্নিত করার জন্য।

### G3 · Sleep / Hibernate সামলানো
**সমস্যা:** ল্যাপটপ বন্ধ করে রাখলে বা PC ঘুমালে সেগমেন্ট খোলা থেকে যেত → **ভুতুড়ে কাজের সময়** যোগ হয়ে যেত।
**সমাধান:** `SystemEvents.PowerModeChanged` (Suspend/Resume) ধরে সেগমেন্ট ঠিকভাবে ক্লোজ করা।

### G4 · Session 0 Isolation — `GetLastInputInfo`-র সীমাবদ্ধতা
**সমস্যা:** শুধু স্ক্রিনশটের জন্য tray app দরকার বলেছিলাম। আসলে **idle detection-ও** সার্ভিস থেকে ভুল ফল দেয় — Session 0-তে ইনপুট বলে কিছু নেই।
**সমাধান:** ট্র্যাকিং ও ক্যাপচার দুটোই ইউজার সেশনে, সার্ভিস শুধু watchdog।
📄 [06-Research §2.2](06-Research.md)

### G5 · ব্যাকআপ থেকে **ফেরানোর** পদ্ধতি
**সমস্যা:** ব্যাকআপ নেওয়ার কথা ছিল, কিন্তু রিস্টোর কীভাবে করব তা কোথাও লেখা ছিল না। সার্ভার PC নষ্ট হলে তখন ভাবতে বসতে হতো।
**সমাধান:** ৭ ধাপের রিস্টোর রানবুক + **ত্রৈমাসিক রিস্টোর ড্রিল বাধ্যতামূলক**।
📄 [02-Workflow §10](02-Workflow.md)

---

## 🟡 B · গুরুত্বপূর্ণ ফাঁক

### G6 · স্টাফের নিজস্ব ভিউ — বলা ছিল, বানানোর পরিকল্পনা ছিল না
"স্বচ্ছতার জন্য স্টাফ নিজের ডেটা দেখতে পারবে" লিখেছিলাম, কিন্তু `users` টেবিলে `employee` role ছিল না, কোনো পেজও ছিল না। → `role='employee'`, `MyData.tsx`, tray-তে "আমার আজকের হিসাব" যোগ হলো (F: J01–J05)।

### G7 · অনবোর্ডিং ও অফবোর্ডিং
নতুন স্টাফ এলে বা কেউ চলে গেলে কী করতে হবে — ধাপে ধাপে লেখা ছিল না। বিশেষত **চলে যাওয়া স্টাফের ডিভাইস revoke** ও ডেটার কী হবে।
📄 [02-Workflow §6](02-Workflow.md)

### G8 · সার্ভার হেলথ মনিটরিং
এজেন্ট পাহারা দেওয়ার ব্যবস্থা ছিল, কিন্তু **সার্ভার নিজে** ঠিক আছে কি না দেখার কেউ ছিল না। ডিস্ক ভরে গেলে বা ব্যাকআপ ব্যর্থ হলে চুপচাপ ফেল করত। → `alerts` টেবিল + health job (ডিস্ক ৮০%/৯৫%, ব্যাকআপ ব্যর্থতা, এজেন্ট নীরবতা)।

### G9 · টেস্টিং কৌশল
কোনো টেস্ট প্ল্যানই ছিল না। টাইম ট্র্যাকিংয়ের নির্ভুলতা হাতে যাচাই না করলে ভরসা করা যায় না। → ৩ স্তরের অটো টেস্ট + **১১ পয়েন্টের ম্যানুয়াল চেকলিস্ট** (স্টপওয়াচ মিলিয়ে দেখা সহ)।
📄 [02-Workflow §9](02-Workflow.md)

### G10 · এজেন্টের রিসোর্স বাজেট
"হালকা হবে" বলেছিলাম, কিন্তু সংখ্যা ছিল না। ১৫টা PC-তে ভারী এজেন্ট বসালে স্টাফরা প্রথম দিনেই অভিযোগ করত। → CPU < ১%, RAM < ১২০ MB, নেটওয়ার্ক < ৩০০ KB/৫ মি. — মাপা হবে (F: H07)।

### G11 · Auto-update + rollback
আপডেটের কথা এক লাইনে ছিল। ১৫টা PC-তে খারাপ আপডেট গেলে সবগুলো একসাথে অচল হয়ে যেত। → হ্যাশ যাচাই, রাতে সাইলেন্ট ইনস্টল, ৫ মিনিটে heartbeat না এলে **অটো rollback**, ধাপে ধাপে রোলআউট (১ → ৩ → সব)। `agent_versions` টেবিল যোগ।

### G12 · একাধিক ইউজার / RDP
এক PC দুজন ব্যবহার করলে, বা কেউ RDP দিয়ে ঢুকলে কী হবে — ভাবা হয়নি। DXGI ক্যাপচার RDP-তে কাজও করে না। → session-aware ট্র্যাকিং, `windows_username` ম্যাপিং, RDP-তে GDI ফলব্যাক।

### G13 · সংবেদনশীল অ্যাপে স্ক্রিনশট
কেউ ব্যাংকিং সাইট বা পাসওয়ার্ড ম্যানেজার খুললে সেই স্ক্রিনশট সার্ভারে জমা হয়ে যেত — অপ্রয়োজনীয় ও বিপজ্জনক। → অ্যাপ/ডোমেইন ব্ল্যাকলিস্ট, তালিকাভুক্ত থাকলে ছবি স্কিপ (F: A11)।

### G14 · DPI awareness
4K বা স্কেল করা মনিটরে (১২৫%/১৫০%) DPI-unaware অ্যাপের ছবি **কাটা পড়ে বা ঝাপসা** আসে। → `PerMonitorV2` DPI-aware ম্যানিফেস্ট বাধ্যতামূলক।

---

## 🟢 C · মাঝারি ফাঁক

| # | যা বাদ পড়েছিল | এখন |
|---|---|---|
| G15 | **সাফল্যের মাপকাঠি** — কীভাবে বুঝব সিস্টেম সফল? | ৮টি মাপযোগ্য টার্গেট ([01 §1](01-Planning.md)) |
| G16 | **Definition of Done** — প্রতিটা ফেজ কখন "শেষ"? | প্রতি ফেজে যাচাইযোগ্য শর্ত |
| G17 | **ঝুঁকি রেজিস্টার** — মাত্র ৫টা ঝুঁকি ছিল | ১২টি, সম্ভাবনা ও প্রভাব সহ |
| G18 | **Enrollment নিরাপত্তা** — যে কেউ এজেন্ট বসিয়ে রেজিস্টার করতে পারত | একবার ব্যবহার্য কোড, ২৪ ঘণ্টায় expire |
| G19 | ~~**Approval SLA**~~ | ⚠️ অপ্রাসঙ্গিক — দফা ৪-এ পুরো অনুমোদন ব্যবস্থাই বাদ |
| G20 | ~~**Half-day নিয়ম**~~ | ⚠️ অপ্রাসঙ্গিক — দফা ৩-এ দৈনিক টার্গেটই বাদ। দিন নয়, ঘণ্টা গোনা হয় |
| G21 | **Rate limiting** | লগইনে ব্রুট-ফোর্স প্রতিরোধ (F: I11) |
| G22 | **Docker ব্যর্থ হলে?** | NSSM দিয়ে নেটিভ ফলব্যাক পথ ([ADR-008](05-Options-Decisions.md)) |
| G23 | **AV exclusion স্ক্রিপ্ট** | `ops/av-exclusion.ps1` ডেলিভারেবল হিসেবে যোগ (F: H09) |
| G24 | **Idle threshold রিভিউ করব কীসের ভিত্তিতে?** | ভুল idle-এর সংখ্যা রেকর্ড হবে → ১ মাস পর ডেটা দেখে সিদ্ধান্ত |

---

## 🔄 D · আপনার নতুন উত্তরে যা বদলাল

| আগে ধরেছিলাম | এখন |
|---|---|
| শুক্র + শনি ছুটি (৫ দিন সপ্তাহ) | ✅ **শুধু শুক্রবার ছুটি — সপ্তাহে ৬ দিন** |
| — | ✅ **মাসে ২৬ দিন × ৮ঘ = ২০৮ ঘণ্টা** মাসিক টার্গেট |
| লাঞ্চ উইন্ডো ১:০০–২:০০ ট্র্যাক হবে | ✅ **লাঞ্চ ট্র্যাকিং সম্পূর্ণ বাদ** |
| BREAK স্টেট, `break_sec` কলাম, break বাটন | ✅ সব বাদ — যা active নয়, তা শুধু "কাউন্ট হয়নি" |
| শিফট = কাজের সময় | ✅ শিফট = **উইন্ডো** (৯–৬), টার্গেট = **৮ ঘণ্টা active** |
| — | ✅ `shortfall_sec`, `overtime_sec`, `target_met_at` কলাম যোগ |
| — | ✅ Tray-তে লাইভ কাউন্টার: "৫ঘ ৪২মি / ৮ঘ" |
| স্টোরেজ ২২ কর্মদিবস ধরে | ✅ ২৬ দিন ধরে — ৫.৬ GB/মাস, ৯০ দিনে ১৭ GB |

**একটা ভালো পার্শ্বপ্রতিক্রিয়া:** লাঞ্চ ট্র্যাকিং বাদ দেওয়ায় ডিজাইন সহজ হয়ে গেছে — একটা স্টেট কমেছে, একটা টেবিল-কলাম কমেছে, স্টাফের উপর অকারণ নজরদারিও কমেছে।

---

## 🔄 D2 · দ্বিতীয় দফা পরিবর্তন — শিফট সম্পূর্ণ বাদ

**আপনার নির্দেশনা:** *"day shift count koro na, diner jekono time e active thakolei hobe. monthly target 208 hours."*

| আগে | এখন |
|---|---|
| শিফট উইন্ডো ৯:০০–১৮:০০ | ❌ **কোনো শিফট নেই** — ২৪ ঘণ্টা গণনা |
| দৈনিক ৮ ঘণ্টা টার্গেট | ✅ **মাসিক ২০৮ ঘণ্টা** — একমাত্র মাপকাঠি |
| দেরি (late) ফ্ল্যাগ, grace ১৫ মিনিট | ❌ বাদ — আসার নির্দিষ্ট সময়ই নেই |
| আগে চলে যাওয়া (early leave) | ❌ বাদ |
| শুক্রবার ট্র্যাকিং বন্ধ | ✅ শুক্রবারে কাজ করলেও গোনা হবে |
| অ্যাটেনডেন্স গ্রিড (present/late/absent) | ✅ **মাসিক হিটম্যাপ** — প্রতিদিন কত ঘণ্টা |
| `shifts` টেবিল, `OFF_SHIFT` স্টেট | ❌ বাদ |
| — | ✅ `work_policies` + `monthly_summary` টেবিল |
| — | ✅ **গতি (pace)** — মাসের মাঝপথে কে এগিয়ে, কে পিছিয়ে |

**নতুন ফাঁক ধরা পড়ল (G25):** সময়ের বাঁধন তুলে দিলে **স্ক্রিনশটও ২৪ ঘণ্টা** উঠবে — রাত ২টায় কেউ ল্যাপটপে ব্যক্তিগত কাজ করলে সেই ছবিও সার্ভারে জমা হবে। এটা আপনার দরকার নেই, আর আইনি "আনুপাতিকতা" নীতির বিরুদ্ধেও যায়।

**✅ সমাধান অনুমোদিত:** সময় গণনা ২৪ ঘণ্টা, **স্ক্রিনশট শুধু ০৭:০০–২৩:০০**। বিস্তারিত: [ADR-011c](05-Options-Decisions.md)।

---

## 🔄 D3 · তৃতীয় দফা পরিবর্তন — অনুমোদন ব্যবস্থা সম্পূর্ণ বাদ

**আপনার নির্দেশনা:** *"ami ei anumodon section ta caina. only time tracker enough. 1 min inactive e hold hobe."*

| আগে | এখন |
|---|---|
| Approvals পেজ (মিটিং / ফিল্ড ওয়ার্ক / ছুটি) | ❌ **বাদ** |
| tray-তে "Meeting/Call" বাটন | ❌ বাদ — স্টাফের চাপার মতো কোনো বাটনই নেই |
| MEETING স্টেট | ❌ বাদ — এখন মাত্র ৩টি স্টেট: **ACTIVE · IDLE · LOCKED** |
| `time_claims` টেবিল | ❌ বাদ |
| `leave_types` `leave_requests` `leave_balances` | ❌ বাদ |
| ৪৮ ঘণ্টার auto-approve নিয়ম | ❌ বাদ |
| `/agent/claims` ও `/claims/:id/approve` endpoint | ❌ বাদ |
| `worked_sec = active + approved meeting` | ✅ `worked_sec = active` — এটুকুই |
| `holidays` টেবিল | ✅ থাকছে, শুধু হিটম্যাপে দিন চিহ্নিত করতে |

**ফলাফল — পুরো লজিক এখন এক লাইনে:**
```
কি-বোর্ড/মাউস ব্যবহার হচ্ছে?  →  হ্যাঁ: ⏱ গোনা হচ্ছে  ·  না (১ মিনিট): ⏸ hold
```
কারো কিছু চাপতে হয় না, কারো কিছু অনুমোদন করতে হয় না।

**যে দাম দিতে হচ্ছে:** দীর্ঘ মিটিং বা ডকুমেন্ট পড়ার সময় idle গণ্য হবে, সংশোধনের উপায় নেই। **তবে মাসিক ২০৮ ঘণ্টার মডেলে এটা বড় সমস্যা নয়** — একদিন কম হলে অন্যদিন পুষিয়ে নেওয়া যায়। দৈনিক টার্গেট থাকলে এটা সমস্যা হতো।

**ডাটাবেস টেবিল:** ২২ → **১৭**। ADR: [ADR-011d](05-Options-Decisions.md)

---

## 🔧 F · Phase 1 শুরুর সময় ধরা পড়া ফাঁক

### G26 · সার্ভার PC-তে প্রয়োজনীয় সফটওয়্যার নেই
পুরো প্ল্যানে ধরে নেওয়া হয়েছিল Docker আর .NET SDK আছে। যাচাই করে দেখা গেল **দুটোরই নেই**।
এটা Phase 1-এর প্রথম ব্লকার — ডাটাবেস ছাড়া migration চালানো যায় না।
📄 বিস্তারিত ও ইনস্টল লিংক: [09-Build-Log](09-Build-Log.md)

| সফটওয়্যার | দরকার | অবস্থা |
|---|---|---|
| Node.js 20+ | সার্ভার ও ওয়েব | ✅ v24.18.0 আছে |
| Git | সোর্স কন্ট্রোল | ✅ v2.55.0 আছে |
| Docker Desktop | Postgres + API | ❌ **নেই** |
| .NET 8 SDK | এজেন্ট (Phase 2) | ❌ **নেই** |

### G27 · ডাটাবেস পোর্ট বাইরে খুলে রাখার ঝুঁকি
docker-compose-এ `5432:5432` লিখলে পুরো LAN থেকে ডাটাবেসে ঢোকা যেত।
**সমাধান:** `127.0.0.1:5432:5432` — শুধু সার্ভার PC থেকেই অ্যাক্সেসযোগ্য।

### G28 · `.env` ভুল করে কমিট হয়ে যাওয়া
পাসওয়ার্ড ও JWT সিক্রেট git-এ চলে যেতে পারত।
**সমাধান:** `.gitignore`-এ `.env` ও `.data/`, আর `.env.example`-এ শুধু প্লেসহোল্ডার।

---

## 🔬 G · দ্বিতীয় দফা রিভিউ — কোড শুরুর ঠিক আগে (G29–G40)

> **কেন আরেকবার:** উপরের ফাঁকগুলো ধরা হয়েছিল **দফা ৩ ও ৪-এর আগে**। শিফট বাদ দেওয়া
> ([D2](#-d2--দ্বিতীয়-দফা-পরিবর্তন--শিফট-সম্পূর্ণ-বাদ)) আর অনুমোদন ব্যবস্থা বাদ দেওয়া
> ([D3](#-d3--তৃতীয়-দফা-পরিবর্তন--অনুমোদন-ব্যবস্থা-সম্পূর্ণ-বাদ)) — এই দুটো সিদ্ধান্ত নিজেরাই
> **নতুন ফাঁক তৈরি করেছে**, যেগুলো কেউ ধরেনি। কোড শুরুর আগে পুরো প্ল্যান আবার মিলিয়ে ১২টা পাওয়া গেল।

### 🔴 G.1 · যেগুলো ভুল হিসাব দিত — **সমাধান স্পেকে বসানো হয়েছে**

| # | ফাঁক | কী ভুল হতো | সিদ্ধান্ত |
|---|---|---|---|
| **G29** | **Idempotency key ডাটাবেসেই ছিল না।** [02 §1](02-Workflow.md) ও [07 §3.5](07-Technical-Spec.md)-এ "client UUID দিয়ে idempotent insert" লেখা, কিন্তু DDL-এ `activity_segments` `app_usage` `events` কোথাও কলামটাই নেই | অফলাইন queue রি-আপলোড হলেই **ঘণ্টা ডাবল** — আর `dedupe.service.ts`-এর dedupe করার মতো কিছুই থাকত না | তিনটি টেবিলে + `screenshots`-এ `client_uuid UUID NOT NULL UNIQUE`; `ON CONFLICT DO NOTHING`; রেসপন্সে `{accepted, duplicates}` → [07 §2.1-ঘ](07-Technical-Spec.md) |
| **G30** | **মধ্যরাত পার হওয়া সেগমেন্ট।** `work_date` একটাই DATE, অথচ শিফট তুলে দেওয়ায় রাতে কাজ এখন স্বাভাবিক | ২৩:৪০–০০:২০ কাজ করলে পুরো ৪০ মিনিট **এক তারিখে** বসত; পরের দিন শূন্য | এজেন্ট ঠিক ০০:০০-এ সেগমেন্ট ও সেশন ভাগ করে (`day_rollover`); সার্ভারে রক্ষাকবচ। **দিন-ক্লোজ জব ২৩:৩০ → ০০:১৫** → [07 §2.1-ক](07-Technical-Spec.md) |
| **G31** | **`workdays_elapsed`-এর কোনো সংজ্ঞা ছিল না** — অথচ `expected = 208 × (workdays_elapsed / 26)` সূত্রে ব্যবহৃত। "কর্মদিবস" বলে তো আর কিছুই নেই | প্রতিটি কার্ডের "এগিয়ে/পিছিয়ে" চিপ **অর্থহীন**; মকআপে সংখ্যাটা হাতে বসানো ছিল | কর্মদিবস = সাপ্তাহিক ছুটি নয় **এবং** `holidays`-এ নেই এমন দিন। `expected_workdays` মাসভিত্তিক গোনা হয় (হার্ডকোড ২৬ নয়) → pace ঠিক মাসের শেষ কর্মদিবসে ২০৮-এ পৌঁছায় → [07 §2.1-খ](07-Technical-Spec.md) |
| **G32** | **একজন স্টাফের একাধিক ডিভাইস।** `worked_sec = Σ ACTIVE` — ডেস্কটপ ও ল্যাপটপ একসাথে চললে | সময় **দুইবার** গোনা হতো। O7-তে "১ PC = ১ স্টাফ" ধরা ছিল, উল্টোটা কেউ ভাবেনি | যোগফল নয়, **UNION** — interval merge; `activity_segments.device_id` যোগ; দিনে ১৫ মিনিটের বেশি overlap হলে `device_overlap` অ্যালার্ট → [07 §2.1-গ](07-Technical-Spec.md) |
| **G33** | **পাসওয়ার্ড রিসেট বলে কিছু ছিল না** — ১৫ জনের self-view লগইন হবে, কেউ ভুলে গেলে কোনো পথ নেই | Auth মডিউল Phase 1-এর কাজ — পরে যোগ করতে গেলে schema বদলাত | `users.must_change_pw` + `POST /auth/change-password` + owner-এর `POST /users/:id/reset-password` (temp পাসওয়ার্ড, SMTP লাগে না) + `POST /employees/:id/portal-account` |
| **G34** | **API কনট্রাক্টে ৪টা endpoint ও ১টা টেবিল অনুপস্থিত** — enrollment code কোথায় জমা হয় তার টেবিলই ছিল না; auto-update ফ্লো ([02 §8](02-Workflow.md)) থাকলেও MSI নামানোর endpoint নেই; সই করা পলিসি রাখার জায়গা নেই। সাথে `/shifts` CRUD এখনো তালিকায় | Phase 1 ও 6-এর মডিউল স্কোপ ভুল হতো | নতুন টেবিল `enrollment_codes`; `GET /agent/update` + `/download`; `POST /devices/enrollment-code`; `POST /employees/:id/policy-doc`; `/shifts` → `/work-policies` |
| **G35** | **সিস্টেমের নিজের দোষে হারানো ঘণ্টা ফেরানোর উপায় নেই।** [ADR-011d](05-Options-Decisions.md) স্টাফের claim বাদ দিয়েছিল — যুক্তিসঙ্গত। কিন্তু এজেন্ট ৩ দিন বন্ধ থাকলে বা সার্ভার ক্র্যাশ করলে স্টাফ **নির্দোষ হয়েও** ঘণ্টা হারাত, ওনারেরও ঠিক করার কোনো বাটন ছিল না | **পে-রোল বিরোধের সবচেয়ে বড় ঝুঁকি।** ADR-011d স্টাফমুখী approval নিয়ে ভেবেছিল, সিস্টেম-ব্যর্থতার মেরামত নিয়ে নয় | নতুন টেবিল `time_adjustments` — শুধু owner, `reason` বাধ্যতামূলক, কাঁচা সেগমেন্টে হাত পড়ে না, heartbeat-গ্যাপ থেকে প্রস্তাবিত পরিমাণ, স্টাফ নিজেও কারণসহ দেখে। `worked_sec` (কাঁচা) ও `credited_sec` (টার্গেটে যায়) আলাদা → [07 §2.1-ঙ](07-Technical-Spec.md) · [ADR-011e](05-Options-Decisions.md) |

**ফলাফল:** ডাটাবেস টেবিল **১৭ → ১৯**, ingest-এ ব্যাচ সীমা/ফাইল সীমা/rate limit যোগ, দিন-ক্লোজ জবের সময় বদল, `worked_sec` ও `credited_sec` আলাদা হলো।

### 🟡 G.2 · খোলা সিদ্ধান্ত — কোড আটকাবে না, রোলআউটের আগে চাই

| # | ফাঁক | কেন গুরুত্বপূর্ণ | প্রস্তাব |
|---|---|---|---|
| **G36** ✅ | **এজেন্ট বন্ধ হলে স্টাফ জানতেই পারে না** — অ্যালার্ট শুধু ওনারের কাছে যায় | স্টাফ সারাদিন কাজ করে জানল না যে কিছুই রেকর্ড হয়নি | **ফিচার হিসেবে যোগ হলো** — [04-Features J07](04-Features.md): সিঙ্ক ব্যর্থ হলে tray আইকন লাল + "সার্ভারে পৌঁছাচ্ছে না, ডেটা লোকালি জমছে" টুলটিপ (Phase 2) |
| **G37** ✅ | **মাঝপথে জয়েন/রিজাইনে proration নেই** — ২০ তারিখে জয়েন করা লোকও পুরো ২০৮ ঘণ্টার টার্গেট পায় | প্রথম মাসেই সে "পিছিয়ে" দেখাবে | **কোডে বসেছে ১২ আগস্ট** — টার্গেট = তার কর্মদিবস × ৮ঘ, বেতন = মাসিক × d÷D ([ADR-025](05-Options-Decisions.md) · [09 § ৩ভ](09-Build-Log.md))। ⚠️ বেতন ও টার্গেট **দুটোই** prorate, নইলে ঘণ্টার হার দ্বিগুণ হতো। ⚠️ মাঠে চালু হবে স্টাফের `joined_on` বসানোর দিন (O13) · ১৩ আগস্ট seed-ও তারিখ নিতে শেখানো হয়েছে ([09 § ৩ষ](09-Build-Log.md)) |
| **G38** | **`counts_as_work` কে সেট করে লেখা ছিল না** | ঝুলে থাকা কলাম | স্পেকে বসানো হলো: `= (state = 'active')`, ingest-এ সেট হয় |
| **G39** | **ব্যাকআপ অফসাইট নয়, এনক্রিপটেডও নয়।** `D:` → `E:` — একই ঘরে। আগুন/চুরিতে দুটোই যাবে। ক্লাউড কপি "ঐচ্ছিক" লেখা, আর এক্সটার্নাল ড্রাইভে এনক্রিপশনের কথা কোথাও নেই — অথচ ওতে ১৫ জনের স্ক্রিনশট | পে-রোলের একমাত্র সূত্র যেই ডেটা | সাপ্তাহিক এনক্রিপটেড অফসাইট কপি **বাধ্যতামূলক** করা; এক্সটার্নাল HDD-তে BitLocker To Go |
| **G40** ✅ | **স্টোরেজ প্রক্ষেপণ পুরোনো ধারণায়** — "৮ ঘণ্টা × ১২ ছবি/ঘণ্টা" ধরে ১৭ GB। কিন্তু ক্যাপচার উইন্ডো এখন ১৬ ঘণ্টা (০৭–২৩) | কেউ ১২ ঘণ্টা কাজ করলে ছবি দেড়গুণ | **ঠিক করা হয়েছে** — [07 §1](07-Technical-Spec.md) ও [06 §5.1](06-Research.md)-এ "সাধারণ ৮ঘ / ভারী ১২ঘ" দুটো কলাম। সবচেয়ে খারাপ ক্ষেত্রে ২৫ GB = 1 TB-র ২.৫% |

### 🔧 G.3 · কোড লিখতে ও **চালাতে** গিয়ে ধরা পড়া (G41–G77)

| # | ফাঁক | অবস্থা |
|---|---|---|
| **G41** | **`capture_now` / `pause_tracking` কমান্ড রাখার জায়গা নেই।** [07 §4.1](07-Technical-Spec.md) heartbeat-এর রেসপন্সে ৫টি কমান্ডের কথা বলে, কিন্তু ড্যাশবোর্ড থেকে চাপা "এখনই ছবি নাও" বাটনটা কোথাও **জমা থাকে না** — এজেন্ট তো ৩০ সেকেন্ড পরে জিজ্ঞেস করতে আসে | ⏳ একটা `device_commands` টেবিল লাগবে (A09 · Phase 6)। `reload_config` ও `revoke` অবস্থা থেকেই বের করা যায়, তাই সেগুলো এখনই কাজ করে |
| **G42** ✅ | **Prisma-র `DateTime` ডিফল্টে `timestamp`, `timestamptz` নয়** — স্পেকের DDL-এ TIMESTAMPTZ লেখা থাকলেও স্কিমায় সেটা বলা হয়নি। raw SQL-এ টাইমজোন হিসাব ৬ ঘণ্টা ভুল দিত | সব instant কলামে `@db.Timestamptz(3)` বসিয়ে নতুন migration নেওয়া হয়েছে |
| **G43** ✅ | **অফলাইন queue রিপ্লেতে সেশন ভেঙে যেত** — পুরোনো ব্যাচ নতুনের পরে এলে চলতি সেশন অতীতের সময়ে বন্ধ হয়ে যেত (`ended_at < started_at`) | সেশন এখন (device, work_date) ধরে খোঁজা হয়; পুরোনো সেশন বন্ধ হয় তার নিজের মধ্যরাতে |
| **G44** ✅ | **`GetLastInputInfo`-র টাইমস্ট্যাম্প ভবিষ্যতে থাকতে পারে।** Microsoft নিজেই বলে dwTime "not guaranteed to be incremental"। মাত্র ৫ সেকেন্ড এগিয়ে থাকলেই সরল বিয়োগ **৪৯.৭ দিনের** ভুয়া নিষ্ক্রিয়তা দিত — ওই স্টাফের সারাদিনের কাজ মুছে যেত। ৬৪-বিটে নিলেও ঠিক হয় না (সমস্যাটা wrap নয়, unsigned underflow) | `IdleMath`-এ ক্ল্যাম্প + টেস্ট, আর কত বড় ভুল হতো সেটাও টেস্টে ধরা আছে যেন কেউ শর্তটা "অপ্রয়োজনীয়" ভেবে মুছে না ফেলে |
| **G45** ✅ | **ঘুম ধরতে ইভেন্টের উপর ভরসা করা যায় না।** ব্যাটারি ফুরিয়ে বা তাপজনিত কারণে PC ঘুমালে Windows **কোনো নোটিফিকেশনই পাঠায় না**। ল্যাপটপ ৫টায় বন্ধ, ৯টায় খোলা = ১৬ ঘণ্টার ভুয়া কাজ | `SleepGapDetector` — biased/unbiased/monotonic তিন ঘড়ি মিলিয়ে ইভেন্ট ছাড়াই ফাঁক ধরে। ইভেন্ট শুধু দ্রুত জানার উপায়, সত্যের উৎস নয় |
| **G47** ✅ | **স্টোরেজের হিসাব প্রতি স্টাফে একটা ছবি ধরেছিল, প্রতি মনিটরে নয়।** অথচ A02 শুরু থেকেই মাল্টি-মনিটর চায় — দুই মনিটরে ছবির সংখ্যা দ্বিগুণ | ঠিক করা হয়েছে; সাথে **মাপা** আকার বসানো হলো: ১৯২০×১০৮০ WebP q70 = **~৭২ KB**, অনুমান ছিল ১৫০ KB। দুটো ভুল অনেকটা কাটাকাটি হয়ে গেছে ([07 § ১](07-Technical-Spec.md)) |
| **G48** ✅ | ⭐ **ক্যাপচার API সফল হওয়া মানে ছবি পাওয়া নয়।** DXGI-র `AcquireNextFrame` সফল হয়, HRESULT ঠিক, মাপও ঠিক (১৯২০×১০৮০, pitch ৭৬৮০) — কিন্তু `AccumulatedFrames == 0` হলে ওই ফ্রেমে ডেস্কটপের ছবিই বসানো হয় না, **প্রতিটা পিক্সেল শূন্য**। এই মেশিনেই মেপে দেখা। ফেরত মান দেখে সন্তুষ্ট হলে মাসের পর মাস নিখুঁতভাবে কালো ছবি জমত, আর "স্টাফের পর্দা সত্যিই কালো ছিল"-র থেকে আলাদা করা যেত না | খালি ফ্রেম ছেড়ে দিয়ে আসল ছবির অপেক্ষা করা হয়। "কিছুই নড়েনি" আলাদা করে চেনা হয়, যাতে শান্ত মেশিনে DXGI অকারণে বিরতিতে না যায় ([ADR-012c](05-Options-Decisions.md)) |
| **G49** ✅ | ⭐ **এক অক্ষরের ভুলে পে-রোল ডেটা মুছে যেত।** এজেন্ট সেগমেন্ট স্টেট পাঠাচ্ছিল বড় হাতে (`"ACTIVE"`), সার্ভারের Prisma enum ছোট হাতের, আর `ValidationPipe`-এ `forbidNonWhitelisted: true`। ফল ৪০০ — আর outcome classifier ৪০০-কে `Permanent` ধরে, অর্থাৎ রিট্রাই নয়, **মুছে ফেলা**। শিক্ষা: তিন-ভাগের classifier যত ভালোই হোক, wire ফরম্যাট ভুল হলে সেটাই ডেটা ধ্বংসের হাতিয়ার হয় | এক জায়গায় ঠিক (`SyncWire.StateToWire`)। সেই সাথে অজানা স্টেটের ডিফল্টও সরানো — আগে অচেনা স্টেট নীরবে `ACTIVE` হয়ে **কাজের সময় হিসেবে গোনা হতো** |
| **G50** ✅ | ⭐ **একটা খারাপ রেকর্ড ৪৯৯টা ভালো রেকর্ড ডোবাত।** সার্ভার পুরো ব্যাচ একসাথে যাচাই করে — ৫০০টার একটার `windowTitle` সীমার বেশি হলে **পুরো ব্যাচেই** ৪০০, আর ৪০০ মানে `Permanent` = মুছে ফেলা। ১৯ জনের ঘণ্টা হারাত একজনের বেঠিক সারির জন্য, আর সার্ভারের দিক থেকে সব স্বাভাবিক দেখাত | `BatchNarrowing` — Permanent পেলে ব্যাচ অর্ধেক (৫০০→১, ৮ ধাপ), একা পড়ে গিয়েও প্রত্যাখ্যাত হলে **তখনই** শুধু সেটা বাদ। নিজেরা যখন খারাপ চিনি (payload পড়া গেল না) তখন ভাগ না করে সরাসরি বাদ |
| **G51** ✅ | **ব্যর্থ মনিটর নীরবে বাদ পড়ত।** `ScreenCaptureService` ছবি না পেলে ওই মনিটর তালিকা থেকে ফেলে দিত। একটা পর্দা চিরতরে ছবি দেওয়া বন্ধ করলেও বাকিগুলোর ছবি আসতে থাকত, তাই সব ঠিক দেখাত — ধরা পড়ত কেবল কেউ ওই পর্দার ছবি খুঁজতে গিয়ে | `LastFailedMonitors` — কোন মনিটর কিছুই দিল না সেটা এখন বলা হয়। আসল রানেই একবার ঘটেছিল |
| **G52** ✅ | **এজেন্ট মাসের হিসাব জানে না।** tray-তে "x ঘ / ২০৮ঘ" দেখানোর কথা, কিন্তু এজেন্ট শুধু নিজের চালু থাকার সময় গোনে। রিবুট বা আপডেটের পর সেটা শূন্য — স্টাফ দেখত মাসের কাজ মুছে গেছে। যে ফিচারের উদ্দেশ্যই আস্থা, সেটাই আস্থা ভাঙত | সংখ্যাটা heartbeat-এর উত্তরে সার্ভার পাঠায়, কাঁচা `activity_segments` থেকে যোগ করে — একাধিক PC ব্যবহার করলেও যোগ হয়ে আসে (§ ২.১-গ) |
| **G53** ✅ | ⭐ **টানা কাজ করলে কিছুই পাঠানো হতো না।** সেগমেন্ট বন্ধ হয় কেবল স্টেট বদলালে, তাই একটানা ACTIVE থাকলে রেকর্ডটা ঘণ্টার পর ঘণ্টা মেমরিতে খোলা থাকত — আর ক্র্যাশ বা বিদ্যুৎ গেলে পুরোটাই হারাত (`CloseAll` কেবল স্বাভাবিক বন্ধে চলে)। **মাপা:** ৩ মিনিট টানা কাজে সার্ভারে শূন্য সেগমেন্ট | সর্বোচ্চ ৫ মিনিটে ভাগ, স্ক্রিনশটের স্লটের সাথে মিলিয়ে। ⚠️ ভাগের সীমানা `now − idleThreshold`-এর আগে রাখতে হয়, নইলে retro-adjust (B04) পিছিয়ে গিয়ে কাটার জায়গা পেত না আর **নিষ্ক্রিয় সময় কাজ হিসেবে গোনা** হতো |
| **G54** ✅ | **টোকেন স্টোর নিজের ফাইল নিজেই লিখতে পারত না।** `device.dat.tmp` বানিয়ে তাতে `Users : Read` ACL বসিয়ে তারপর write করতে গিয়ে "Access denied"। কোডের মন্তব্যে ধারণাটা লেখাই ছিল — "ইনস্টলার লেখে, এজেন্ট পড়ে" — কিন্তু **enrollment এজেন্টই করে** | যে অ্যাকাউন্টে এজেন্ট চলে সেটাই Modify পায়। `Users : Read` সরানো হয়েছে — DPAPI LocalMachine স্কোপে ওটা মানে ছিল ওই PC-র যেকোনো ইউজার টোকেন খুলে ফেলতে পারত |
| **G55** ✅ | ⭐ **ব্যর্থ লেখার কারণে enrollment কোড খরচ হয়ে যেত।** কোড একবার-ব্যবহার্য: সার্ভার সেটা খরচ করে ডিভাইস বানিয়ে টোকেন দেয় — **একবারই**। ডিস্কে লেখা ব্যর্থ হলে কোডও গেল, টোকেনও গেল, সার্ভারে অনাথ ডিভাইস রইল। ঠিক করতে অ্যাডমিনের নতুন কোড লাগত। আসল মেশিনে ঘটেছে | `CanPersist()` — একই ACL, একই পথে প্রোব ফাইল লিখে **আগে** নিশ্চিত হওয়া, তারপর সার্ভারে যাওয়া |
| **G56** ✅ | **এজেন্ট ভুল ঠিকানায় কথা বলত।** sync client চায় API-রুট (`/api/v1` সহ), অথচ `AgentSettings`-এ লেখা ছিল খালি ঠিকানা দিতে। প্রতিটা রিকোয়েস্টে ৪০৪ — অর্থাৎ এজেন্ট চলত, tray উঠত, কিন্তু কিছুই পাঠাত না | prefix এজেন্ট নিজে জুড়ে নেয়। সার্ভারের ভেতরের ব্যাপার অফিসের অ্যাডমিনকে মনে রাখতে বলা মানে একদিন কেউ ভুলবে |
| **G57** ✅ | ⭐ **watchdog একটার পর একটা এজেন্ট চালু করত।** watchdog মডিউল একটা liveness চুক্তি ঠিক করেছিল — এজেন্ট `agent.lock` ধরে রাখবে আর প্রতি ১৫ সে. `agent.alive` লিখবে। `AgentHost`-এ ওই দিকটা **বানানোই হয়নি**। ফলে watchdog প্রতি ৩০ সেকেন্ডে ভাবত এজেন্ট মরে গেছে, আর নতুন একটা চালু করত। দুটো এজেন্ট = **একই ঘণ্টা দুবার গোনা**, আর সার্ভারে সেটা "কেউ বেশি কাজ করছে" ছাড়া আলাদা দেখাত না | `LivenessBeacon` — লক ধরে (single-instance guard-ও ওটাই) আর unbiased ঘড়ি দিয়ে হার্টবিট লেখে। ⚠️ biased ঘড়ি দিলে ল্যাপটপ ঘুমিয়ে ওঠার পর হার্টবিট "১০ ঘণ্টা পুরোনো" মনে হতো আর সুস্থ এজেন্টকেই মারা হতো |
| **G58** ✅ | ⭐ **আপগ্রেড ও আনইনস্টল দুটোই চিরতরে আটকে যেত (১৬০৩)।** মেজর আপগ্রেডে MSI **দুটো** সেশন চালায় — নতুনটা বসানো, আর পুরোনোটা `REMOVE=ALL` দিয়ে সরানো। `SERVERURL` লঞ্চ-শর্তটা দ্বিতীয় সেশনেও চলত, যেখানে ওই প্রপার্টি থাকার কথাই নয়। একই কারণে `msiexec /x`-ও ব্যর্থ হতো — অর্থাৎ প্রোডাক্টটা **কোনোদিন সরানোই যেত না** | `Installed OR SERVERURL` — পাশের Windows-ভার্সন শর্তে এই রক্ষাকবচ আগে থেকেই ছিল, এটাতে দিতে ভুলে গিয়েছিলাম |
| **G59** ✅ | **আপগ্রেডের পর `devices.agent_version` পুরোনোই থেকে যেত।** ওটা শুধু enrollment-এ বসত। ⚠️ সমস্যাটা ড্যাশবোর্ডের সংখ্যার চেয়ে গভীর: heartbeat handler **ওই স্টেল মান দেখেই** ঠিক করত আপডেট অফার করবে কি না — অর্থাৎ আপডেট হয়ে যাওয়া এজেন্টকেও **একই আপডেট বারবার অফার** করা হতো, আর সে আপডেট করে আবার heartbeat পাঠাত। H04-এর ধাপে ধাপে রোলআউট ও rollback দুটোই এর উপর দাঁড়ানো | এজেন্ট heartbeat-এ নিজের ভার্সন পাঠায়; সার্ভার **আগে** সেটা বসায়, **তারপর** আপডেটের সিদ্ধান্ত নেয়। ⚠️ শুধু বদলালে তবেই লেখা (নইলে ১৫ ডিভাইসে দিনে ২১,৬০০ অপ্রয়োজনীয় UPDATE), আর ভার্সন না পাঠালে আগেরটা **মুছে যায় না** — পুরোনো এজেন্ট ফিল্ডটা চেনে না, তাতে আপডেট অফারই বন্ধ হয়ে যেত। দুটোরই টেস্ট আছে |
| **G60** ✅ | **`appName` সার্ভারের `@MaxLength(260)` ছাড়িয়ে পুরো ব্যাচকে ৪০০ করাতে পারত।** ওটা আসে এক্সিকিউটেবলের version resource-এর `FileDescription` থেকে — **আমাদের লেখা নয়, আর কোথাও বাঁধা নেই**। ৪০০ মানে Permanent, অর্থাৎ G49-এর মতোই **ডেটা মুছে ফেলা**। (`windowTitle` probe-এ ৫১২-তে, `domain` parser-এ ২৫৩-তে বাঁধা ছিল — কিন্তু সেটা দুটো আলাদা ফাইলের আচরণের উপর নির্ভরশীল, চুক্তি নয়) | `SyncWire`-এ, তারে ওঠার ঠিক আগে **চারটে স্ট্রিংয়েরই** দৈর্ঘ্য ছাঁটা হয় — যেখানে ফুল-URL ছাঁটাইটাও হয়। ⚠️ উৎসে ছাঁটা হয়নি ইচ্ছাকৃতভাবে: নতুন কোনো উৎস যোগ হলে সে-ও একই দরজা দিয়েই যাবে |
| **G61** ✅ | ⭐ **মডিউল লেখা হলো, import হলো, কিন্তু `imports`-এ যোগ করা হয়নি।** `ActivityModule` তৈরি, controller-ও আছে — অথচ `app.module.ts`-এ শুধু import স্টেটমেন্ট, অ্যারেতে নাম নেই। **Nest এতে কোনো অভিযোগ করে না**: মডিউলটা রানটাইমে থাকেই না, `/categories` আর `/activity/*` সব ৪০৪ দেয়, অথচ কোড দেখে সবকিছু সম্পূর্ণ মনে হয়। ধরা পড়েছে কেবল `no-unused-vars` লিন্ট রুলে | `imports`-এ যোগ। যাচাইয়ের উপায়: রুটগুলো এখন **৪০১** দেয় (লগইন লাগে), ৪০৪ নয় |
| **G62** ✅ | **টেস্ট দিনের বেলা পাস করত, রাত ১২টার পর ফেল।** dedupe টেস্টগুলো `minutesAgo(30)` দিয়ে ফিক্সচার বানাত; ঢাকার মধ্যরাতের ঠিক পরে চালালে সেটা আগের তারিখে পড়ত, সার্ভার নিয়ম মেনে মধ্যরাতে ভাগ করত (§ ২.১-ক), আর `accepted` ২-এর বদলে ৩ আসত। সিস্টেমের মূল নিয়মই যেখানে ঢাকার মধ্যরাত, সেখানে তারিখ-নির্ভর ফিক্সচার সময়ের বোমা | `todayWindow()` — যেটা ঠিক এ কারণেই বানানো, আর ফাইলের অন্য টেস্টে ব্যবহারও হচ্ছিল |
| **G63** ✅ | **Live Board-এর স্ট্যাটাস heartbeat থেকে আসত না।** `HeartbeatDto.state` নেওয়া হতো কিন্তু কোনো টেবিলে লেখা হতো না, তাই রঙ অনুমান করা হতো শেষ `activity_segments` সারি থেকে — আর এজেন্ট সেগমেন্ট **ব্যাচে** পাঠায়, তাই ৩০ সেকেন্ডে রিফ্রেশ হওয়া বোর্ড কয়েক মিনিট পিছিয়ে থাকত | `devices.last_state` / `last_state_at`, heartbeat-এর একই `UPDATE`-এ। ⚠️ **রিপোর্টেরও মেয়াদ দিতে হয়েছে**: এজেন্ট মরার আগে `active` বলে গেলে মানটা কলামে বসে থাকত আর বন্ধ PC-র কার্ড **চিরকাল সবুজ** দেখাত — অর্থাৎ না-কাজের সময় কাজ বলে দাবি করা হতো |
| **G64** ✅ | ⭐ **থাম্বনেইলের কোড সম্পূর্ণ, তবু কোনোদিন চলত না।** `FileInterceptor('file')` multipart-এর **একটাই** অংশ নেয়, তাই এজেন্টের পাঠানো `thumb` অংশটা multer নীরবে ফেলে দিত — `ingest()`-এ পৌঁছাতই না, `thumb_path` চিরকাল null থাকত। কোনো এরর নেই, কোনো লগ নেই; গ্যালারি ফুল ছবিতে ফেরত যেত আর সব ঠিক দেখাত | `FileFieldsInterceptor([{file},{thumb}])`। ⚠️ দুটোতেই `maxCount: 1` — `limits.fileSize` **প্রতি ফাইলে** খাটে, মোটে নয় |
| **G65** ✅ | **heartbeat-এর `progress` এজেন্ট কোনোদিন পড়েনি।** `SyncWire.HeartbeatResponseDto`-তে `Progress` ঘরটাই ছিল না; STJ অচেনা ফিল্ড চুপচাপ ফেলে দেয়, তাই tray-তে "এ মাসে" **চিরকাল ০ ঘণ্টা** — অথচ সার্ভার প্রতিটা heartbeat-এ সংখ্যাটা পাঠাচ্ছিল। J03 ও J04 দুটোই এর উপর দাঁড়ানো | ঘরটা যোগ, আর তারের দিকে **সব ফিল্ড nullable** (`ProgressDto`) — `EmployeeProgress`-এ সরাসরি deserialize করলে তার একটা `required` সদস্য অনুপস্থিত হলেই **পুরো heartbeat উত্তর** (revoke কমান্ড সহ) পড়া যেত না |
| **G66** ✅ | **`ApplicationExit`-এর `async void` হ্যান্ডলারে `DisposeAsync` কখনো শেষ হতো না।** প্রথম `await`-এই হ্যান্ডলার ফিরে আসত, WinForms ধরে নিত কাজ শেষ, প্রসেস মরত — ফলে খোলা সেগমেন্ট বন্ধ করা, `agent_stop`, শেষ drain কিছুই চলত না। অর্থাৎ G02 কাগজে সম্পূর্ণ, বাস্তবে নিষ্ক্রিয় | `Application.Run()` ফেরার পর **সিঙ্ক্রোনাস** `Shutdown()`, ৪ সে.-এর ছাদসহ; drain-এর ছাদ ১০ → ৩ সে. (বাইরেরটার ভেতরে)। সাথে `WM_ENDSESSION`-এও `agent_stop` বসে, কারণ `Application.Run()` আদৌ ফিরবে কি না সেটা WinForms-এর অভ্যন্তরীণ ব্যাপার |
| **G67** ✅ | **ব্যাকআপের DB পাসওয়ার্ড হোস্টের প্রসেস-তালিকায় দেখা যেত।** Docker মোডে `docker exec -e PGPASSWORD=<মান>` লেখা ছিল, অর্থাৎ পাসওয়ার্ডটা `docker` প্রসেসের argv-তে বসত — আর argv **যেকোনো** লোকাল ব্যবহারকারী `ps aux` দিয়ে পড়তে পারে, রোজ রাত ২:৩০-এ। কোডের মন্তব্যে লেখা ছিল "বিকল্প নেই" | বিকল্প আছে, আর যাচাই করা হয়েছে: `-e PGPASSWORD` (শুধু **নাম**) দিলে `docker exec` মানটা নিজের env থেকে তুলে নেয়, ঠিক `docker run`-এর মতো। argv-তে আর কিছু থাকে না |
| **G68** ✅ | **`AuditAction` ইউনিয়নে 2FA-র নামগুলো ছিল না**, তাই একটা সেতু-ফাইল (`two-factor.audit.ts`) cast দিয়ে টাইপ মেলাচ্ছিল। রেকর্ড ঠিকই লেখা হতো, কিন্তু টাইপো লিখলে সেটা **নীরবে** `audit_log`-এ বসে যেত আর কোনো ফিল্টারে ধরা পড়ত না | আটটা নাম (+ `backup_run`) আসল ইউনিয়নে; সেতু-ফাইল মুছে দেওয়া হয়েছে। `TwoFactorAuditAction` এখন `Extract<AuditAction, \`2fa_${string}\`>` — আলাদা তালিকা নয়, তাই দুটো আর কখনো আলাদা হতে পারে না |
| **G69** ⚠️ | **H04-এ রোলব্যাক নেই — ইচ্ছাকৃতভাবে।** নামানো ও sha256-যাচাই স্বয়ংক্রিয়, কিন্তু **বসানো নয়**। কারণ G58: খারাপ MSI একবার চললে নতুন MSI দিয়ে ফেরানো যায় না, হাতে যেতে হয় — ১৫টা PC-তে রাত ৩টায় সেটা ঘটলে সকালে পুরো অফিসের ট্র্যাকিং বন্ধ | `halted` দিয়ে বিলি থামানো যায়, কিন্তু যারা পেয়ে গেছে তাদের ফেরাতে হাতে যেতে হবে। সত্যিকারের rollback করতে watchdog-কে **আগের MSI ধরে রাখতে ও ফিরিয়ে বসাতে** হবে — সেটা এখনো নেই |
| **G70** ✅ | ⭐ **কর্মীদের নাম ও বেতন `seed.ts`-এ হার্ডকোড করা ছিল**, আর সেটা ধরা পড়েছে GitHub-এ পুশ করার ঠিক আগে। ⚠️ এটা মালিকের নিজের তথ্য নয় — **কর্মীদের**, আর তাঁরা জানেনও না। private রিপোতেও ডেটা তৃতীয় পক্ষের সার্ভারে যায়, collaborator যোগ করলে সে-ও দেখে। ⚠️ আর git-এ একবার ঢুকলে শুধু নতুন কমিটে মুছে লাভ নেই — ইতিহাসের প্রতিটা ট্রি-তে থেকে যায় | তালিকা `staff.local.json`-এ (gitignore), seed সেখান থেকে পড়ে; না পেলে নমুনা দিয়ে **চলে**, থামে না — নইলে কেউ বিরক্ত হয়ে আবার হার্ডকোড করত। ইতিহাসের ৩৬টা কমিট `filter-branch` দিয়ে নতুন করে লেখা, তারপর reflog ও gc |
| **G71** ✅ | ⭐ **A07 — ছবির পাশে অ্যাপের নাম চিরকাল খালি ছিল।** পুরো পথটা তৈরি বলে মনে হয়: `screenshots.active_app`/`active_title` কলাম আছে, `agent/dto.ts` মান দুটো নেয় (`@MaxLength` সহ), `screenshot-ingest.service.ts` লেখে, `screenshots.service.ts` ফেরত দেয়, ওয়েবের **তিন** জায়গা দেখায় (গ্যালারির গ্রিড · লাইটবক্স · staff পাতার `DayShots`), আর এজেন্টের তারেও ঘর দুটো আছে (`ScreenshotMetaDto.ActiveApp`)। ⚠️ **শুধু উৎসেই কেউ মান বসায় না** — `AgentHost.CaptureSlotAsync`-এ `ScreenshotRecord`-এর আটটা ঘরের ছটা ভরা হয়, `ActiveApp`/`ActiveTitle` বাদ। দুটোই nullable, তাই কম্পাইলার চুপ, সার্ভার চুপ, লগ চুপ — শুধু লাইটবক্সে চিরকাল `—`। ⚠️ ধরা পড়েনি কারণ এজেন্টের নিজের ছবি এখনো সার্ভারে পৌঁছায়নি, আর নমুনা ডেটার সারি ingest-এর পথ দিয়ে আসেনি | `AppUsageTracker.Current` (নতুন) → `AppUsageService.Current` → `CaptureSlotAsync`। ⭐ **নতুন করে Win32-কে জিজ্ঞেস করা হয় না** — যা app-usage টিক শেষবার পড়েছে সেটাই নেওয়া হয়, নইলে ছবির পাশের নাম আর `app_usage`-এর সারি দুই নমুনা থেকে এসে আলাদা হতে পারত ("ছবিতে Excel, রিপোর্টে Chrome")। ⚠️ পড়াটা লুপের **বাইরে**: ছবি প্রতি মনিটরে একটা, foreground উইন্ডো একটাই — ভেতরে পড়লে মাঝপথে উইন্ডো বদলালে একই মুহূর্তের দুই ছবিতে দু-রকম নাম বসত। ⚠️ কনফিগে অ্যাপ ট্র্যাকিং বন্ধ থাকলে `_apps` তৈরিই হয় না, তাই নামও বসে না — "পাঠাচ্ছি না" নয়, **জানাই হয় না**। সাথে `SyncWire.ScreenshotMeta`-তে G60-এর `Clamp` (২৬০ · ১০০০) — এতদিন ওখানে ছিল না, আর ৪০০ মানে Permanent, অর্থাৎ লম্বা টাইটেলের দায়ে **ছবিটাই মুছে যেত**। ৫টা নতুন টেস্ট। ⭐ **সেই রাতেই মাঠে প্রমাণিত** ([09 § ৩ঐ](09-Build-Log.md)): আসল ক্যাপচারে দুটো সারিতেই `active_app = explorer.exe`, আর `active_title` null — কারণ ওই মুহূর্তের `app_usage` সারিটাও বলছে উইন্ডোটার টাইটেল খালি ছিল। ⭐ **দুটো একই নমুনা থেকে আসাটাই প্রমাণ** যে আলাদা probe না চালানোর সিদ্ধান্তটা কাজ করেছে। ✅ টাইটেল **ভরা** অবস্থাও এসেছে ১৯:৪৪:২৮-এ (`chrome.exe`, ৩১ অক্ষর) — আর তাতে ADR-013-ও যাচাই হলো: `active_title`-এ `http` বা path নেই |
| **G72** ✅ | ⭐ **ড্যাশবোর্ডের Settings কোনোদিন এজেন্টে পৌঁছাত না — দুটো আলাদা কারণে।** ১· `GetConfigAsync()` ইন্টারফেসে ছিল, `HttpSyncClient`-এ বাস্তবায়িতও ছিল — কিন্তু **কেউ ডাকত না**। ২· heartbeat হ্যান্ডলার সার্ভারের `configVersion` **অন্ধভাবে** নিজের ঘরে বসিয়ে দিত, তাই পরের heartbeat-এ ভার্সন মিলে যেত আর সার্ভার আর কোনোদিন `reload_config` চাইত না। ⚠️ **দ্বিতীয়টা প্রথমটাকে ঢেকে রাখত** — এজেন্ট চিরকাল `AgentConfig.Default`-এ চলত (idle সীমা · স্লট · ছবির সময়সীমা সবই), অথচ সার্ভারের চোখে ডিভাইসটা হালনাগাদ। ১৫টা PC-র একটাও কিছু জানত না | `NeedsConfig()` — কমান্ড **অথবা** ভার্সন না মিললে আনা হয়। ⚠️ দ্বিতীয় শর্তটা নিছক বেল্ট-অ্যান্ড-ব্রেসেস নয়: সদ্য চালু হওয়া এজেন্টের ভার্সন `null`, আর তখন সার্ভার কোনো কমান্ড পাঠায় না ("কিছু বদলায়নি") — শুধু কমান্ডে ভরসা করলে **রিবুটের পর** এজেন্ট চিরকাল ডিফল্টেই চলত। ⭐ প্রয়োগ হয় `TrackLoop`-এ, heartbeat থ্রেডে নয় — ট্র্যাকিংয়ের অবজেক্টগুলো ওই থ্রেডের, বাইরে থেকে ছুঁলে একই সেকেন্ডগুলো দুই কনফিগে ভাগ হয়ে **ঘণ্টা হারাত**। নতুন `ConfigChange` (+টেস্ট) যা বদলেছে শুধু সেটুকুই প্রয়োগ করে, নইলে কেউ Settings-এ save চাপলেই সবার চলতি সেগমেন্ট অকারণে কাটা পড়ত। মাঠে যাচাই: `idle 60→90` ও ছবি `23:00→21:00` → [09 § ৩খ](09-Build-Log.md) |
| **G73** ✅ | ⭐ **ড্যাশবোর্ড প্রোডাকশনে চালানোর ব্যবস্থাই ছিল না।** `docker-compose.yml`-এ সার্ভিস ছিল দুটো — `postgres` আর `api`। ওয়েব চলত কেবল `npm run dev`-এ, অর্থাৎ রানবুক মেনে সার্ভার দাঁড় করালে **ড্যাশবোর্ড বলে কিছুই থাকত না**। ⚠️ আলাদা origin-এ বসালেও চলত না: `web/src/api/client.ts`-এ `BASE = '/api/v1'` (আপেক্ষিক পথ), আর সেশন cookie `sameSite: 'strict'` — লগইন সফল দেখাত, পরের রিকোয়েস্টেই ৪০১ | নতুন `web/Dockerfile` (node build → Caddy) ও `web/Caddyfile`। Caddy `/api/*` কে `api:3000`-এ পাঠায়, বাকি পাতা নিজে দেয় → ব্রাউজারের চোখে **একটাই origin**, তাই CORS-এর প্রশ্নই ওঠে না (`CORS_ORIGIN` তখন কেবল অন্য ক্লায়েন্টের জন্য)। ⚠️ `try_files {path} /index.html` বাধ্যতামূলক — রুটিং ক্লায়েন্টে, নইলে `/staff/7`-এ refresh চাপলেই ৪০৪, আর সেটা ধরা পড়ত রোলআউটের দিনে। পোর্ট `WEB_PORT` (ডিফল্ট 8080) |
| **G74** ✅ | ⚠️ **রানবুক মেনে চললে ডাটাবেসে একটাও টেবিল বসত না** — `docker compose up -d`-এর পর migrate/seed ধাপটাই কোথাও লেখা ছিল না। ⚠️ কেউ নিজে থেকে খুঁজেও বের করতে পারত না: api-র runtime ইমেজে `npm ci --omit=dev`, তাই prisma CLI বা tsx কোনোটাই নেই — কনটেইনারের ভেতর থেকে চালানোই যেত না | নতুন one-shot সার্ভিস: `docker compose --profile setup run --rm migrate`। ⭐ `target: build` স্তরে চলে, কারণ ওখানেই সোর্স ও পূর্ণ node_modules দুটো আছে। ⭐ profile দেওয়া বলে `up -d`-তে চলে না — মাইগ্রেশন একটা **সচেতন ধাপ**, প্রতি রিস্টার্টে নিজে থেকে চলার জিনিস নয়। ⚠️ `npm run seed` নয়: ওটা `dotenv -e ../.env` দিয়ে মোড়া আর কনটেইনারে ওই ফাইলটাই নেই |
| **G75** ✅ | **`.env.example`-এ ছিল ১০টা চলক, সার্ভার ও seed মিলিয়ে পড়ে ৩১টা।** বাদ পড়া তালিকায় পুরো SMTP সেট (৬টা) ও `ALERT_EMAIL_TO`, সাথে ব্যাকআপের চলকগুলো। ⚠️ এতে কিছু **ভাঙত না**, আর সেটাই বিপদ — অ্যালার্ট তৈরি হতো, `alerts`-এ বসত, ড্যাশবোর্ডে দেখাত, শুধু **কারো কাছে পৌঁছাত না**। "SMTP নেই" মানে চ্যানেল চুপচাপ বন্ধ, ভুল নয় | `.env` থেকে আসার কথা এমন সবগুলো এখন `.env.example`-এ, আর compose-এর `api` environment-এও পাস করা হয় — নইলে `.env`-এ বসিয়ে কেউ ভাবতেন কাজ হচ্ছে। (`NODE_ENV` · `PORT` · `STORAGE_ROOT` ইচ্ছাকৃতভাবে compose-এ স্থির, ওগুলো অ্যাডমিনের বদলানোর জিনিস নয়) |
| **G76** ✅ | **`api`-তে healthcheck ছিল না** — ছিল কেবল `postgres`-এ। রানবুক বলে "দুটোই healthy হওয়া চাই", অথচ `docker compose ps` api-র জন্য শুধু `running` দেখাত — আর ভেতরে অ্যাপ ক্র্যাশ-লুপে থাকলেও সেটা running-ই | `/api/v1/health` ডেকে `db === 'up'` না হলে unhealthy। ⭐ scheme `TLS_CERT` দেখে বাছা হয়: TLS চালু হলে ওই পোর্টে আর প্লেইন HTTP থাকে না, তাই স্থির `http://` লিখলে সার্ট বসানোমাত্র কনটেইনার **চিরকাল unhealthy** দেখাত। এর উপরেই `web`-এর `depends_on: service_healthy` দাঁড়ানো |
| **G77** ⚠️ | **ডকের দাবিটাই ভুল ছিল: watchdog কোনো Windows Service নয়।** `oXeio.Watchdog`-এ `ServiceBase` বা `System.ServiceProcess`-এর নামগন্ধ নেই — ওটা `--install-task` দিয়ে বসানো একটা **লগঅন Scheduled Task**। ⚠️ ভুলটা শুধু পরিভাষার নয়: "সার্ভিস" ধরে নিলে `sc stop`/services.msc দিয়ে ভদ্রভাবে থামানোর কথা ভাবা হতো, যেটা নেই — MSI নিজেও এজেন্ট ও watchdog দুটোকেই `taskkill /F` দিয়ে মারে (`Package.wxs`) | ডক ঠিক করা হয়েছে ([09 § ৩ঈ](09-Build-Log.md))। ⚠️ **সমাধান নয়, নথিবদ্ধ সীমা** — এজেন্ট ভদ্রভাবে থামানোর পথ এখনো নেই, তাই G66-এর সাজানো shutdown (খোলা সেগমেন্ট বন্ধ · `agent_stop` · শেষ drain) MSI-র পথে চলেই না |
| **G78** ✅ | ⭐⭐ **ইনস্টলার লগঅন-টাস্ক বানাত, কিন্তু চালু করত না।** `InstallLogonTask` ঠিকই চলত (`Get-ScheduledTask` → Ready), কিন্তু টাস্কের ট্রিগার **লগঅন** — তাই MSI "সফল" বলে বেরিয়ে যেত আর **পরের লগঅন বা রিবুট পর্যন্ত কিছুই হতো না**: কোনো সাইন-ইন জানালা নয়, কোনো ঘণ্টা গোনা নয়, কোনো এরর নয়। ⚠️ বাইনারিতে কোনো দোষ ছিল না — হাতে `oXeio.Watchdog.exe` চালালেই সব কাজ করত। আবারও **"চুক্তি লেখা আছে, কলার লেখা হয়নি"**, শুধু এবার কলারটা একটা ফাংশন নয় — **প্রথম রানটা** | `StartWatchdog` কাস্টম অ্যাকশন, `After="InstallFinalize"` ([09 § ৩ধ](09-Build-Log.md))। ⚠️ `schtasks /Run` কাজ করে **না** — principal `Group=Users` (H02), Group টাস্কের একক ইউজার নেই তাই on-demand চালানো যায় না। ⚠️⚠️ তিনটে খুঁটিনাটি: `immediate` (deferred হলে SYSTEM-এর সেশন ০-তে, জানালা কেউ দেখত না) · `asyncNoWait` (watchdog কোনোদিন বেরোয় না, নইলে ইনস্টলার চিরকাল ঝুলত) · InstallFinalize-এর **পরে** (আগে হলে `StopWatchdog`-এর taskkill সবে-চালু প্রসেসটাই মারত)। ০.৩.২ |
| **G79** ✅ | ⭐⭐ **সাইন ইন করার আগেই এজেন্ট গোনা ও ছবি তোলা শুরু করত।** ইনস্টলের পর সাইন-ইন জানালা খোলা অবস্থাতেই tray দেখাত সবুজ "Working"। `StartLoops()` ট্র্যাকিং ও ক্যাপচার লুপ চালু করত `EnrollIfNeededAsync`-এর **সমান্তরালে**, কোনো শর্ত ছাড়াই। ⚠️ মালিকের মেশিনে ১৮ মিনিটে জমেছিল: **২টা আসল স্ক্রিনশট** (৪০ ও ৬৯ KB), **১৪টা AppUsage**, **৩টা Segment** — কেউ সাইন ইন না করা অবস্থায়। ⚠️⚠️ তিনটে আলাদা ক্ষতি: ১· **ভুল লোকের নামে ঘণ্টা** — সাইন ইন করামাত্র ডিভাইসটা ওই কর্মীর নামে বাঁধা পড়ত আর আগের সব সারি তার খাতায় যেত (অ্যাডমিন PC বসিয়ে কাজ করলে সেটাও) · ২· **সম্মতির আগেই ছবি** · ৩· **জানালাটা মিথ্যে বলত** — সবুজ বিন্দু মানে সব ঠিকঠাক, অথচ একটা বাইটও সার্ভারে যেতে পারত না, তাই স্টাফের সাইন ইন করার কথাই মাথায় আসত না | নতুন Core নিয়ম `TrackingGate` (`Allowed`/`NotEnrolled`/`Revoked`) — TrackLoop, AppUsageLoop, CaptureGate ও দুটো tray পর্দা সবাই **একই** নিয়ম মানে ([09 § ৩প](09-Build-Log.md))। ⚠️ `AppUsageLoop`-টা প্রথম দফায় বাদ পড়েছিল আর **মেপে দেখাতেই** ধরা পড়ে (৭ মিনিটে একটা সারি: `oXeio.Agent.exe`, ৩০০ সে. — সাইন-ইন জানালা খোলা থাকার সময়টুকুই); আলাদা থ্রেড, নিজের সারি নিজেই কিউতে ফেলে। ⚠️ ক্রম: revoke আগে, কারণ revoke টোকেন মোছে বলে তখন দুটো শর্তই সত্যি — উল্টো হলে বাতিল মেশিনে লেখা থাকত "সাইন ইন করুন"। ⭐ `AgentStatus.Enrolled` ইচ্ছাকৃতভাবে `required`, যাতে কম্পাইলারই "কলার লেখা হয়েছে কি না" পাহারা দেয়। ০.৩.৪ |
| **G80** ✅ | ⭐⭐ **সাইন ইন করার কোনো উপায় ছিল না।** সাইন-ইন জানালাটা আসত **শুধু চালু হওয়ার সময়**, একবার — বন্ধ করে দিলে ফেরার একমাত্র পথ ছিল লগ-অফ করে আবার লগ-ইন। ⚠️ **G79** ঠিক করার **আগে** ফাঁকটা চোখেই পড়ত না, কারণ এজেন্ট এমনিতেই গুনত; G79 সাইন-ইনটা বাধ্যতামূলক করার পর প্রথমবার দেখা গেল দরজাটা নেই। ⚠️⚠️ আর বার্তাটা ব্যাপারটা আরও খারাপ করেছিল — পর্দায় লাল হরফে *"Sign in to start counting your hours"*, অথচ মানার কোনো পথ নেই। **যে নির্দেশ করার উপায় দেখায় না, সেটা নির্দেশ নয় — দোষারোপ** | tray মেনুতে **"Sign in…"** (`Visible`, `Enabled` নয় — সাইন ইন হলে আইটেমটার মানে নেই) + `TrayOptions.RequestSignIn` + `SignInOnDemandAsync()`। ⚠️ `Interlocked` পাহারা — স্টার্টআপ ও মেনু দুটো পথই একসাথে ডাকা যায়, নইলে দুটো জানালা পাশাপাশি খুলত। বার্তায় এখন **কোথায়** সেটাও আছে ([09 § ৩ফ](09-Build-Log.md))। ০.৩.৫ |
| **G81** ✅ | ⭐⭐ **VPS-এ স্ক্রিনশট জমা হচ্ছে না — অথচ গ্যালারিতে সারি দেখা যাচ্ছে।** মালিকের পর্দায় "10 this day", প্রতিটাই ভাঙা ছবি; Live Board-এ "Link expired — refresh"। ⚠️⚠️ **সারি আছে, ফাইল নেই** — এই জোড়াটাই সূত্র। ingest আগে DB-তে সারি লেখে, তারপর ডিস্কে ফাইল ([screenshot-ingest.service.ts:113 vs :155](../oxeio-monitor/server/src/agent/screenshot-ingest.service.ts))। **সন্দেহভাজন কারণ:** `Dockerfile`-এ `chown -R node:node /data/storage` করা আছে, কিন্তু compose ঠিক ওই পথেই হোস্টের ফোল্ডার mount করে — আর **mount ইমেজের ফোল্ডারটা মালিকানাসহ ঢেকে দেয়**। ডকার হোস্টে ফোল্ডারটা **root**-এর নামে বানায়, কনটেইনার চলে `node` (uid 1000) হয়ে → লেখায় permission denied। ⚠️⚠️ **ব্যর্থতাটা নীরব হলো যেভাবে:** লেখা ব্যর্থ → এজেন্ট রিট্রাই → এবার DB বলে duplicate (P2002) → সার্ভার `accepted: 0, duplicate: true` অর্থাৎ **সফল** ফেরত দেয় → এজেন্ট আউটবক্স থেকে মুছে ফেলে। ছবিটা চিরতরে হারায়, আর কোথাও একটাও অ্যালার্ট ওঠে না | 🔍 **নির্ণয় হয়েছে, মালিকের নিশ্চিত করা বাকি** — `docker compose exec api touch /data/storage/.probe` দিয়ে এক কমান্ডেই ধরা পড়ে। তাৎক্ষণিক সমাধান `chown -R 1000:1000 .data/storage`। ⏳ **স্থায়ী সমাধান এখনো বসেনি:** সার্ভার চালুর সময়ই storage-এ লেখা যায় কি না পরীক্ষা করে **জোরে থেমে যাওয়া**, আর duplicate-পথে ফাইলটা সত্যিই আছে কি না দেখা ([09 § ৩স](09-Build-Log.md)) ✅ **১৩ আগস্ট রাতে বসেছে, দুই অংশেই:** (১) চালুর সময় storage-এ সত্যিই একটা probe ফাইল লিখে দেখা — না পারলে সার্ভার **ওঠেই না**, আর বার্তায় `chown -R 1000:1000` লেখা থাকে (⭐ `access(W_OK)` যথেষ্ট নয়: read-only mount বা ভরা ডিস্কে সে সফল বলেও পরে লেখা আটকায়)। (২) duplicate পথে ফাইলটা সত্যিই আছে কি না দেখা — না থাকলে **সারিয়ে দেওয়া** (`accepted: 1`), শুধু ব্যর্থ বলা নয়, নইলে এজেন্ট প্রতিবার একই দেয়ালে ধাক্কা খেত। ⚠️⚠️ লেখা হয় **সারির নিজের `file_path`-এ**, নতুন করে হিসাব করা পথে নয় — নইলে ঠিক যে অমিলটা সারানো হচ্ছে সেটাই আবার তৈরি হতো। ৮টি টেস্ট, তিনটে সংশোধনের আগে ব্যর্থ হয় ([09 § ৩ং](09-Build-Log.md)) |
| **G82** ⏳ | **অফিস "সাইন আউট করেছে" আর "PC বন্ধ" আলাদা করে দেখতে পায় না।** ০.৩.৮-এ স্টাফ tray থেকে সাইন আউট করতে পারেন, কিন্তু খবরটা সার্ভারে যায় না — টোকেন মুছে ফেলার পর heartbeat পাঠানোর উপায়ই থাকে না। ⚠️ ফলে Live Board-এ মেশিনটা শুধু "অফলাইন" দেখাবে, ঠিক যেন কেউ PC বন্ধ করে গেছে। শেয়ার করা PC-তে এটা বিভ্রান্তিকর — মালিক বুঝবেন না ডিভাইসটা এখন **খালি**, নাকি কারো নামে বাঁধা | ⏳ সার্ভারে একটা `POST /agent/sign-out` লাগবে, আর সেটা ডাকতে হবে টোকেন মোছার **আগে** ([09 § ৩হ](09-Build-Log.md))। ⚠️ অফলাইনে ডাকটা ব্যর্থ হবে — তখনও সাইন আউট **হতেই হবে**, নইলে শেয়ার করা PC আটকে যেত |
| **G83** ✅ | ⭐⭐ **অস্থায়ী পাসওয়ার্ড টাইপ করাই যেত না, আর তার শাস্তি ছিল ১৫ মিনিটের তালা।** মালিক স্টাফের পাসওয়ার্ড রিসেট করলেন, স্টাফ এজেন্টের জানালায় কয়েকবার ভুল টাইপ করলেন, আর পর্দায় এল *"Too many failed attempts. Try again in 13 minutes."* — অর্থাৎ মনে হলো **রিসেটটাই কাজ করছে না**, অথচ পাসওয়ার্ড ঠিকই ছিল (e2e টেস্ট তা প্রমাণ করে)। কারণ `base64url`-এ `l` `I` `1` আর `O` `0` পাশাপাশি, সাথে ছোট-বড় হাতের মিশ্রণ — ১৪ অক্ষর পর্দা থেকে পড়ে নির্ভুল টাইপ করা প্রায় অসম্ভব। ⚠️⚠️ ব্যর্থতার বার্তা **আসল কারণের দিকে আঙুল দেখায়নি** — তালার কথা বলেছে, টাইপো নিয়ে একটা শব্দও নয় | দ্ব্যর্থহীন বর্ণমালা: `H7K2-M9PQ-3TVX` — ৩২ অক্ষর (`0 O 1 I l` বাদ, সবই বড় হাতের), ৬০ বিট ([temp-password.ts](../oxeio-monitor/server/src/auth/temp-password.ts) · ৯ টেস্ট)। ⭐ ৮৪ → ৬০ বিট সচেতন বিনিময় — অনলাইনে ২^৬০ এমনিতেই অসম্ভব, আর বদলে জিনিসটা **টাইপ করা যায়**। তালার মাপও `.env`-এ, ডিফল্ট ১০ বার / ২ মিনিট, `LOGIN_LOCK_MINUTES=0` দিলে পুরোপুরি বন্ধ ([09 § ৩ড়](09-Build-Log.md)) |
| **G84** ✅✅ | ⭐⭐ **কর্মী আবার সক্রিয় করলে তাঁর লগইন ফিরত না।** `deactivate()` কর্মীর `users.is_active = false` বসাত, `reactivate()` সেটা ফেরাত না। ফলে Staff পর্দায় কর্মী **Active** দেখাতেন, "Reset password" **সফল** হতো ও নতুন পাসওয়ার্ড দেখাত, অথচ লগইনে সবসময় *"Email or password is incorrect"*। ⚠️⚠️ `login()` এই এক বার্তা তিন কারণে দেয় (ইউজার নেই · `isActive` মিথ্যা · পাসওয়ার্ড ভুল) — user enumeration ঠেকাতে ইচ্ছাকৃত, কিন্তু সেজন্যই আসল কারণ জানার **কোনো উপায় ছিল না**। ⚠️ টেস্টে ধরা পড়েনি কারণ `deactivate` ও `reactivate` **আলাদা আলাদা** পরীক্ষা করা ছিল, জোড়াটা নয় | `reactivate()` এখন এক ট্রানজ্যাকশনে লগইনও ফেরায়, আর audit-এ `portalRestored` লেখে। `resetPassword()` নিষ্ক্রিয় অ্যাকাউন্টে ৪০৯ দেয় ও **কী করতে হবে** বলে (আগে নীরবে সফল হতো)। ⚠️ নিজে থেকে সক্রিয় করে না — সেটা আলাদা সচেতন কাজ। ৮টা নতুন টেস্ট ([09 § ৩ঢ়](09-Build-Log.md))। ⚠️ **পুরোনো ভাঙা সারি কোড সারায় না** — একবার SQL লাগে (deploy/README § ১২.৩) |
| **G85** ✅ | **work policy নিষ্ক্রিয় করা যায়, ফেরানো যায় না।** `work-policies.service.ts` `is_active = false` বসায়, অথচ কোথাও `is_active: true` বসানোর কোড নেই, কন্ট্রোলারে `reactivate` রুটও নেই — একবার নিষ্ক্রিয় করা পলিসি **চিরতরে** নিষ্ক্রিয়, ফেরার একমাত্র পথ SQL। ⭐ **G84 সারানোর পর নতুন নিয়মটা প্রয়োগ করেই পাওয়া গেছে** ("যা বন্ধ করার কোড লিখেছেন, খোলার কোডও লিখুন"), মাঠে ধরা পড়ার আগেই। ⚠️ ক্ষতি G84-এর মতো তীব্র নয় — পলিসি নিষ্ক্রিয় করতে গেলে আগে সব কর্মীকে সরাতে হয়, তাই কেউ আটকে যান না। কিন্তু ভুল করে চাপলে ফেরার পথ নেই, আর পর্দায় সেটা কোথাও লেখা নেই | ⏳ `POST /work-policies/:id/reactivate` — `deactivate`-এর হুবহু উল্টোটা ([09 § ৩ঢ়](09-Build-Log.md)) ✅ **১৩ আগস্ট বসেছে** — `POST /work-policies/:id/reactivate`, owner-only, আগে থেকেই সক্রিয় হলে ৪০৯ (নইলে audit log-এ এমন "বদল" জমত যেখানে কিছুই বদলায়নি)। ⚠️ কর্মী-সংখ্যা আসল গোনা থেকেই আসে, শূন্য ধরে নয় — নিষ্ক্রিয় পলিসিতে কর্মী থাকা সম্ভব, আর তখন পর্দা "0 staff" দেখাত অথচ বাস্তবে তাঁরা আছেন। ৫টি টেস্ট ([09 § ৩ং](09-Build-Log.md)) |
| **G86** ✅ | ⭐⭐ **চলতি সেশন ডাটাবেসের বদল মানত না।** `JwtAuthGuard`-এর sliding window প্রতি ৫ মিনিটে টোকেন নতুন করে দিত, কিন্তু দাবিগুলো **পুরোনো টোকেন থেকে নকল** করত — ডাটাবেস কখনো পড়া হতো না। ফলে ⚠️⚠️ **ছাঁটাই হওয়া কেউ ট্যাব খোলা রাখলে ড্যাশবোর্ড তাঁর কাছে খোলাই থেকে যেত** (বেতন, স্ক্রিনশট, সব), নামিয়ে দেওয়া ম্যানেজার ক্ষমতা ধরে রাখতেন, আর `mustChangePw` চলতি সেশনে খাটত না | রিফ্রেশের সময় ভূমিকা · `is_active` · `mustChangePw` ডাটাবেস থেকে পড়া হয়; অ্যাকাউন্ট না থাকলে বা বন্ধ থাকলে সেশন ওখানেই শেষ। ⭐ খরচ নগণ্য — ৫ মিনিটে একবার, প্রতি রিকোয়েস্টে নয়। ৫টি টেস্ট, একটা সংশোধনের আগে ২০০ পেত ([09 § ৩য়](09-Build-Log.md)) |
| **G87** ✅ | **portal অ্যাকাউন্টের ভূমিকা বদলানোর কোনো পথ ছিল না।** ভূমিকা বসত কেবল অ্যাকাউন্ট খোলার সময়; কাউকে ম্যানেজার করতে হলে অ্যাকাউন্ট মুছে নতুন করে খুলতে হতো — নতুন পাসওয়ার্ড, আর `user_id`-নির্ভর সব audit ইতিহাস ছিঁড়ে যেত | `PATCH /users/:id/role` (owner-only, স্টাফ ↔ ম্যানেজার)। ⚠️ `owner` দুই দিক থেকেই বাইরে — দেওয়া যায় না (ADR-011d), কাড়াও যায় না (নিজেকে নামালে কেউ আর ঢুকতে পারত না)। পর্দার ড্রপডাউন **বর্তমান** ভূমিকা দেখিয়ে খোলে, নইলে শুধু ইমেইল ঠিক করতে গিয়ে একজন ম্যানেজার নীরবে স্টাফ হয়ে যেতেন। ১০টি টেস্ট ([09 § ৩য়](09-Build-Log.md)) |
| **G88** ✅ | ⭐⭐ **কার্ড নিজেই নিজেকে মিথ্যা প্রমাণ করত।** Belal-এর কার্ডে ১৬:৫০-এর স্ক্রিনশট ও ৫৫ মিনিট, অথচ পাশে *"Never checked in"*। Live Board ডিভাইস গোনে শুধু `status: 'active'`, আর ছাঁকাটা কোয়েরিতেই ছিল — তাই "কখনো বসেনি" আর "বসানো ছিল কিন্তু বন্ধ" এক দেখাত। ⚠️ Belal-এর ডিভাইস বাতিল হয়েছিল কারণ তাঁকে একবার নিষ্ক্রিয় করা হয়েছিল (G84-এর একই ঘটনা) | `agentPresence` — তিনটে আলাদা লেখা, কারণ করণীয়ও তিন রকম: `No agent yet` · `Agent switched off` · `Never checked in`। ⚠️ বাতিল ডিভাইসের পুরোনো `lastSeenAt`-ও আর গোনা হয় না, নইলে বন্ধ মেশিন "Seen 7 days ago" দেখাত ([09 § ৩ৎ](09-Build-Log.md)) |
| **G89** ✅ | **"Devices" পর্দাটা সিস্টেমকে জটিল করছিল** — মালিকের কথায় *"ami Devices ei option tai chai na"*। ওই পর্দার চারটে কাজের দুটো কেউ ব্যবহারই করত না (ডিভাইস তালিকা, enrollment কোড — স্টাফ এখন ইমেইল-পাসওয়ার্ডে সাইন ইন করেন), আর বাকি দুটো আসলে **মানুষ-কেন্দ্রিক**: মালিক "ডিভাইস #৬১" নিয়ে ভাবেন না, ভাবেন "Belal-এর PC" নিয়ে | পর্দাটা তুলে দেওয়া। বন্ধ করা → কর্মীকে নিষ্ক্রিয় করুন; ফেরানো → Staff সারিতেই **"Turn agent on"** (`POST /employees/:id/agent/turn-on`, owner-only)। ⚠️⚠️ নিশ্চিতকরণসহ — revoke `token_hash` মোছে না, তাই ফেরালে **পুরোনো টোকেনই জেগে ওঠে**; হারিয়ে যাওয়া ল্যাপটপে করলে যে ধরে আছে সে-ও ফেরে। ৮টি টেস্ট ([09 § ৩ৎ](09-Build-Log.md)) |
| **G90** ✅ | **Live Board-এ সবাই একসাথে ছিল।** বোর্ডের আসল প্রশ্ন "এখন কে কাজ করছে?", অথচ ১৫ জনের অফিসে কাজ করা ৩ জনকে খুঁজে বের করতে চোখ বুলাতে হতো — মালিকের কথায় *"shudhu working staff dekhte cai"* | দুটো ট্যাব — **Working** (ডিফল্ট) ও **Not working**, সংখ্যাসহ। ⚠️ 🔴 agent down চাপা পড়ে না — উপরের লাল টাইল সবসময় থাকে, আর ট্যাবের গায়েও আলাদা লেখা। ⚠️⚠️ ভাগটা **বাদ দিয়ে** গোনা হয়, বেছে নিয়ে নয় — নইলে নতুন কোনো status কোনো ট্যাবেই না পড়ে বোর্ড থেকে মানুষ নীরবে উধাও হতো। উপরের টাইল ও ট্যাব **একই** `isWorking()` ডাকে, নইলে G88-এর মতো বোর্ড আবার নিজেকে কাটত। ৯টি টেস্ট ([09 § ৩ঀ](09-Build-Log.md)) |
| **G91** ✅ | ⭐⭐ **VPS-এ কোনো ফায়ারওয়ালই চলছিল না** — অথচ `vps-setup.sh`-এ ধাপটা লেখা আছে। ধরা পড়েছে বাইরে থেকে পোর্ট স্ক্যান করে: ৩৩০৬ · ৮০৮০ · ৯৯৯৯ সবেতেই সাথে সাথে **RST**, অথচ ufw সক্রিয় থাকলে ওগুলো নীরবে **DROP** হতো (timeout)। ⚠️ কারণ স্ক্রিপ্টের `else` শাখা — ufw ইনস্টল না থাকলে সে শুধু `warn` ছাপত, কিছু করত না, আর ওই এক লাইন ডিপ্লয়ের লগে হারিয়ে গেছে। ⭐ এটা **"চুক্তি লেখা আছে, কলার লেখা হয়নি"**-র দশম ঘটনা, এবার উল্টো মুখে: কলার লেখা ছিল, কিন্তু সে **কিছুই করত না** | স্ক্রিপ্ট এখন **নিজেই `apt-get install ufw`** করে, তারপর ২২ · **২২২২** · ৮০ · ৪৪৩ খুলে enable করে। ⭐ শেষে `ufw status` পড়ে **মিলিয়ে দেখে** — "চালানো হয়েছে" আর "চালু আছে" এক নয়। ⚠️ ক্ষতি সীমিত ছিল কারণ postgres (৫৪৩২) ও api (৩০০০) দুটোই `127.0.0.1`-এ বাঁধা (§ ৩শ ফাঁদ ২), তাই বাইরে থেকে নাগাল ছিল না। ✅ **১৩ আগস্ট বসানো হয়েছে ও বাইরে থেকে যাচাই করা** — বন্ধ পোর্টগুলো RST থেকে **timeout**-এ উল্টে গেছে, অর্থাৎ DROP হচ্ছে; নির্ণয়ের নিয়মটা দুই দিকেই খাটল ([09 § ৩ঃ](09-Build-Log.md)) |
| **G92** ✅ | ⭐⭐ **ISP বাইরের ২২ পোর্ট আটকাচ্ছে — সার্ভারে কিছুই হয়নি।** মালিক আগে SSH-এ ঢুকতে পারতেন, হঠাৎ `Connection timed out`। ⭐ কারণটা `Last login`-এর IP মিলিয়ে বেরিয়েছে: আগের লগইন **অফিস** থেকে (HelloTech AS138640, ২২ খোলা), আটকে যাওয়াটা **বাসা** থেকে (AmberIT AS23956, ২২ বন্ধ) — জায়গা বদলেছে, সেটিং নয়। ⚠️ ফাঁদটা হলো বার্তাটা **সার্ভারের দিকে আঙুল তোলে**, অথচ VPS তখন Let's Encrypt সার্টসহ ২০০ ফেরত দিচ্ছিল। প্রমাণ দুই ধাপে: (১) পাঁচটা ভিন্ন হোস্টের (github · gitlab · bitbucket · codeberg · ssh.github.com) ২২ পোর্টেও timeout; (২) VPS-এর ২১ · ২২২২ · ৩৩০৬ · ৮০৮০ · ৯৯৯৯ সবেতেই **refused**, শুধু **২২ ও ২৫** timeout — আর ২২+২৫ জোড়াটা ISP ফিল্টারের স্বাক্ষর (২৫ স্প্যাম ঠেকাতে, ২২ "নিরাপত্তা") | ⭐ নির্ণয়ের নিয়মটা রানবুকে (§ ১২.৪ক): **খোলা-বন্ধ নয়, timeout বনাম refused দেখুন** — সার্ভার RST দিলে সে সজাগ, অর্থাৎ দোষ পথের, গন্তব্যের নয়। সমাধান তিন স্তরে: VPN (Cloudflare WARP, সাথে সাথে) → sshd-তে **`Port 2222`** যোগ (স্থায়ী, কারণ ২২২২-এ প্যাকেট পৌঁছায়) → ওয়েব কনসোল। ⚠️ `Port 22` লাইনটাও রাখতে হয় — একটাও `Port` লিখলেই ডিফল্ট ২২ চলে যায় ([09 § ৩ঃ](09-Build-Log.md)) |
| **G93** ✅ | **`git status` ১৬৫টা ফাইল "modified" দেখাত, যেখানে একটা অক্ষরও বদলায়নি** — ৫০,২৯৭ যোগ ও ৫০,২৯৭ বাদ, পুরোটাই CR। রিপোটা Windows (মালিকের PC) ও macOS দুই জায়গা থেকেই ছোঁয়া হয়, আর `.gitattributes` ছিল না। ⚠️ আসল বিপদ বিরক্তি নয়, **আড়াল** — ওই ভুয়া ডিফের ভেতরে সত্যিকারের একটা বদল ঢুকে পড়লে রিভিউতে সেটা কেউ দেখত না | `.gitattributes` — `* text=auto eol=lf`, সাথে `*.sh text eol=lf` আলাদা করে। ⚠️ `.sh`-এ CRLF ঢুকলে VPS-এ `$'\r': command not found` — পড়ে কারণ বোঝার উপায় নেই। ⭐ `.ps1`-এর **BOM** এতে অক্ষত থাকে; BOM ফাইলের ভেতরের বাইট, লাইন-এন্ডিং নয় ([09 § ৩ঃ](09-Build-Log.md)) |
| **G94** ✅ | ⭐⭐ **Ubuntu 24.04-এ `sshd_config`-এর `Port` একা কিছুই করে না।** ২২.১০ থেকে SSH চলে **systemd socket activation** দিয়ে (`systemctl is-enabled ssh.socket` → `enabled`), তাই পোর্ট ঠিক করে `ssh.socket`, কনফিগ ফাইল নয়। ⚠️⚠️ ফাঁদটা সম্পূর্ণ নীরব: লাইনটা ফাইলে বসত, `sshd -t` চুপ থাকত, `systemctl restart ssh` সবুজ বলত — আর ২২২২-এ **কিছুই শুনত না**, একটাও এরর ছাড়া। ⭐ ধরা পড়েছে কেবল **বদলানোর আগে অবস্থা মাপায়** | `systemctl disable --now ssh.socket` তারপর `systemctl enable --now ssh` — classic sshd-তে ফেরা, তখন `Port` লাইনগুলো খাটে। ⚠️ `Port 22`-ও রাখতে হয়: একটাও `Port` না থাকলে ডিফল্ট ২২, কিন্তু একটা লিখলেই ডিফল্ট চলে যায়। ✅ **মাঠে প্রমাণিত** — banner `SSH-2.0-OpenSSH_9.6p1`, আর মালিক অফিসের নেটওয়ার্ক থেকেই `ssh -p 2222` দিয়ে ঢুকেছেন ([09 § ৩ঃ](09-Build-Log.md)) |
| **G95** ⚠️ | **VNC কনসোলে যন্ত্রচালিত কীস্ট্রোকে `>` আর `|` পাঠানো যায় না** — noVNC অক্ষরে shift মানে (`SHIFTTEST` ঠিক বসে), চিহ্নে মানে না: `>` → `.`, `|` → `\`, `Ctrl+C` → খালি `c` (তাই `nano`/`vi` থেকে বেরোনোও যায় না)। ⚠️⚠️ রূপটা নীরব — `echo Port 2222 >> file` বসে যায় `echo Port 2222 . file` হয়ে, শেল **কোনো এরর দেয় না**, শুধু লেখাটা ছাপে | রানবুকে § ১২.৪ঘ — কোন অক্ষরগুলো পাঠানো যায় তার তালিকা, আর redirect ছাড়া লাইন যোগ করার পথ `sed -i -e '1iPort 2222' …` (একটাও শিফটেড চিহ্ন লাগে না)। ⚠️ শুধু **যন্ত্রের** সীমা — আসল কীবোর্ডে হাতে টাইপ করলে noVNC ঠিকই পড়ে |
| **G96** ✅ | ⭐⭐ **ওয়েব বিল্ড ১২ ঘণ্টা ভাঙা ছিল, আর CI-র লাল বাতিটা কেউ দেখেনি।** `StaffTab.tsx(921,68): TS2345 — Argument of type 'Role' is not assignable to parameter of type '"manager" | "employee"'`। অর্থাৎ G87-এর কমিট থেকে **ড্যাশবোর্ডের নতুন ইমেজ বানানোই যেত না** — ডিপ্লয় করতে গেলে ওখানেই আটকাত। ⚠️ চোখে পড়েনি কারণ সার্ভার ও এজেন্টের টেস্ট সবুজ, আর ওয়েবের `tsc -b` কেউ স্থানীয়ভাবে চালায়নি | মানটা কখনোই `'owner'` হতে পারত না (initializer ছেঁকে দেয়, owner-এর ড্রপডাউন দেখানোই হয় না) — শুধু **ঘোষিত টাইপটা চওড়া** ছিল। ⭐ সমাধান **উৎসে, cast-এ নয়**: `AssignableRole = Exclude<Role, 'owner'>`, আর `PORTAL_ROLES`-ও সেই টাইপে — কেউ ভুল করে `{ value: 'owner' }` যোগ করতে গেলে কম্পাইলারই থামাবে। ADR-011d এখন DTO **আর** পর্দা, দুই জায়গাতেই বাঁধা ([09 § ৩ঃঃ](09-Build-Log.md)) |
| **G97** ✅ | ⭐⭐ **`web` কনটেইনার প্রথম দিন থেকে মিথ্যা `unhealthy`** — `FailingStreak: 1077`, অথচ সাইট বাইরে থেকে ২০০। কারণ § ৩শ ফাঁদ ৩-এরই পুনরাবৃত্তি **এক স্তর গভীরে**: override `ports` সারিয়েছিল, `healthcheck` বাদ পড়েছিল। বেসেরটা `127.0.0.1:8080`-এ ধাক্কা দেয়, ডোমেইন-মোডে Caddy শোনে `:80`/`:443`-এ। ⚠️ ক্ষতিটা লাল লেখা নয় — `docker compose ps` স্বাস্থ্যের সংকেত হিসেবে **অকেজো** হয়ে যায়, আর `depends_on: service_healthy` কখনোই পূরণ হতো না। ⭐ চিরকাল-লাল অ্যালার্ম মানুষকে অ্যালার্ম উপেক্ষা করতে শেখায় | `healthcheck: !override` — Caddy-র admin API (`127.0.0.1:2019/config/`), যা প্রসেস জীবিত **ও** কনফিগ লোড হওয়া দুটোই প্রমাণ করে, আর বাইরের DNS/TLS লাগে না। ⚠️ `!override` বাধ্যতামূলক (`test` তালিকা, compose তালিকা **যোগ** করে)। ⚠️ পোর্ট ৮০ চলত না: ৩০১ দেয়, আর ইমেজের **BusyBox** wget-এ `--max-redirect` নেই। কনটেইনারের ভেতরে তিনটে probe মেপে বাছা: `:2019` exit 0 · `:80` exit 1 · `:8080` exit 1 ([09 § ৩ঃঃ](09-Build-Log.md)) |
| **G46** ⏳ | **মাউস-জিগলার।** শূন্য-দূরত্বের `SendInput` পুরোপুরি idle টাইমার রিসেট করে, পর্দায় কিছুই দেখা যায় না — ১০ লাইনের কোড। (`SetCursorPos` করে না, তাই কার্সর-সরানো টুল ঝুঁকি নয়) | ⚠️ blueprint low-level keyboard/mouse hook দিয়ে ধরার পরামর্শ দিয়েছিল — **প্রত্যাখ্যান করা হয়েছে**, কারণ ওটা [06-Research § ২.১](06-Research.md) ও [04-Features § L](04-Features.md)-এ নিষিদ্ধ কীলগিং। বদলে: দীর্ঘ অবিচ্ছিন্ন ACTIVE সময় (লক নেই, সেশন বদল নেই, foreground উইন্ডো বদল নেই) রিভিউয়ের জন্য চিহ্নিত হবে |

### 📄 যেসব ফাইল রেফার করা হয়েছে কিন্তু লেখাই হয়নি

| ফাইল | কোথায় রেফার | কখন দরকার |
|---|---|---|
| `docs/monitoring-policy-template.md` | ৪ জায়গায় | **আইনগতভাবে সবচেয়ে গুরুত্বপূর্ণ কাগজ** — Phase 7-এর ব্লকার, এখনই ড্রাফট করা ভালো |
| রিস্টোর রানবুক (আলাদা ফাইল) | [02 §7](02-Workflow.md) ইনসিডেন্ট টেবিল | ৭ ধাপ [02 §10](02-Workflow.md)-এ আছে, আলাদা রানবুক নেই |
| `ops/av-exclusion.ps1` · `ops/deploy-agent.ps1` | [03 §2](03-Project-Map.md) | Phase 6–7 |

---

## ✅ E · যা আগেই ঠিক ছিল

এগুলো নিয়ে দুশ্চিন্তার কিছু নেই — প্রথম ডিজাইনেই ঠিকভাবে ধরা ছিল:

- ৫ মিনিট স্লটে র‍্যান্ডম স্ক্রিনশট — গণিত ও অ্যালগরিদম নির্ভুল
- Idle retro-adjust লজিক (৬০ সেকেন্ড ফিরিয়ে কাটা)
- অফলাইন queue + idempotent আপলোড
- Watchdog দিয়ে এজেন্ট পাহারা
- ৯০ দিনের retention + অটো ডিলিট
- Audit log — কে কার স্ক্রিনশট দেখল
- আইনি/HR প্রস্তুতি ও স্বচ্ছতার নীতি
- স্টোরেজ ও পারফরম্যান্স হিসাব
- টেক স্ট্যাক নির্বাচন
- ফেজভিত্তিক টাইমলাইন

---

## 📊 সারসংক্ষেপ

| | আগে | এখন |
|---|---:|---:|
| ডকুমেন্ট | ২টি | **৯টি** |
| ডেটাবেস টেবিল | ১৩ | **১৯** |
| তালিকাভুক্ত ফিচার | ~৩৫ | **১১৪** (১০৭ সক্রিয় · ৭ বাতিল) |
| চিহ্নিত ঝুঁকি | ৫ | **১২** |
| লিখিত ওয়ার্কফ্লো | ০ | **১০** |
| স্থাপত্য সিদ্ধান্ত (ADR) | অলিখিত | **২২টি লিখিত** *(১টি বাতিল: ADR-012)* |
| চিহ্নিত ফাঁক | অজানা | **৭৭টি** (G1–G77) |
| খোলা প্রশ্ন | অজানা | **৭টি** (O1–O7) + **G36–G40** |

**মূল্যায়ন:** হিসাব ভুল করে দিত এমন ফাঁক আর নেই — **G29–G35 স্পেকে বসানো হয়ে গেছে**, prisma schema লেখা শুরু করা যায়।

⭐ **এই তালিকাটাই শেষ কথা নয়।** ১১ আগস্ট ৭৮টা এজেন্ট ডকের দাবিগুলো **কোডের বিরুদ্ধে**
যাচাই করেছে, আর প্রতিটা দাবি আলাদা এজেন্ট দিয়ে খণ্ডনের চেষ্টাও হয়েছে (৭২টার ৯টা ওখানেই
বাদ পড়েছে)। যা টিকেছে তার পূর্ণ তালিকা [09 § ৩ক](09-Build-Log.md)-এ — G72–G77 ওই
অডিটেরই ফসল। ⚠️ আর অডিট এটাও দেখিয়েছে যে **G32 · G35 · G37**-এর সমাধান এখানে
স্পেকে লেখা আছে ঠিকই, কিন্তু কোডে **অর্ধেক**: `time_adjustments` পড়ার সব স্তর আছে,
কিন্তু **লেখার পথ নেই** (G35) · `device_overlap`-এর টাইপ, লেবেল ও ফিল্টার আছে, কিন্তু
**অ্যালার্ট তোলার কোড নেই** (G32) · proration কেবল রিপোর্টে বসেছে, `monthly_summary`
ও payroll-এ নয় (G37)। "স্পেকে বসানো" আর "চলে" এক কথা নয়।

✅ **G35 ও G32 দুটোই ১২ আগস্ট বন্ধ হয়েছে** — সংশোধন লেখার পথ
([09 § ৩ঙ](09-Build-Log.md)) আর `device_overlap`-এর প্রযোজক
([09 § ৩ঞ](09-Build-Log.md))। ✅ **G37-ও ১২ আগস্ট রাতে বন্ধ হয়েছে।**

⚠️ **G34-এর অর্ধেকও বন্ধ হয়েছে:** সইয়ের **তারিখ** বসানোর রুট আছে
(`POST /employees/:id/policy-signed`), কিন্তু সই করা কাগজের **স্ক্যান
আপলোডের** রুট (`policy-doc`) এখনো নেই।

✅ **G37 কোডে বসেছে** *(১২ আগস্ট রাত)* — টার্গেট = তার কর্মদিবস × ৮ ঘণ্টা,
বেতন = মাসিক × d ÷ D ([ADR-025](05-Options-Decisions.md) ·
[09 § ৩ভ](09-Build-Log.md))। তিনটে খোলা প্রশ্নেরই উত্তর এসেছিল আগে:
O9 (পুরো মাস ছুটি → পুরো বেতন) · O10 (সবখানে, পুরোনো মাস সহ) ·
O11 (শুধু শুক্রবার, তাই migration লাগেনি)।

⚠️⚠️ **তৈরি ≠ চালু।** কোনো স্টাফের `joined_on` বসানো নেই (O13), তাই
নিয়মটা আজও প্রত্যেককে পুরো-মাস ধরে নিচ্ছে। তারিখগুলো বসানোর দিনই
proration সত্যি হবে — আর সেটা কোডের কাজ নয়।

⭐ **১৩ আগস্ট — পথটা অন্তত বানানো হয়েছে।** এতদিন seed-এ যোগদানের তারিখ
নেওয়ার ঘরই ছিল না, অর্থাৎ O13 বন্ধ করার একমাত্র উপায় ছিল ড্যাশবোর্ডে
একজন একজন করে বসানো। এখন `staff.local.json`-এর পঞ্চম ঘরে তারিখ দিলে seed
নিজেই বসিয়ে দেয় ([09 § ৩ষ](09-Build-Log.md))। ✅ **O13 নিষ্পত্তি
(১৩ আগস্ট)** — মালিক তারিখগুলো নিজে পরে বসাবেন; কোডের দিক থেকে করার
কিছু নেই। ⚠️ তারিখ না বসা পর্যন্ত proration **নীরবে নিষ্ক্রিয়** থাকে
(`joined_on` খালি = পুরো মাস, কোনো এরর নয়), তাই প্রথম যে মাসে কেউ
মাঝপথে যোগ দেবেন সেই মাসের হিসাবের **আগে** বসলেই যথেষ্ট।

বাকি যা রোলআউটের আগে চাই: **G39** (অফসাইট এনক্রিপটেড ব্যাকআপ)। **G36** ও **G40** ঠিক করা হয়ে গেছে। আর `monitoring-policy-template.md`-এর ফাঁকা ঘরগুলো না ভরা পর্যন্ত Phase 7 শুরুই করা যাবে না।
